# SIH-BR-MCP: Contexto do Projeto

> **Este arquivo resume as decisões tomadas no planejamento e orienta o Claude Code sobre o estado atual e próximos passos.**

## Objetivo

Desenvolver um **MCP Server** para análise de dados do Sistema de Informações Hospitalares do SUS (SIH-SUS), com foco em **Internações por Condições Sensíveis à Atenção Primária (ICSAP)**.

---

## Progresso do Desenvolvimento

### Passo 1 (Planejamento) ✅
### Passo 2 (Estrutura e Scripts R) ✅
### Passo 3 (Geração de Dados de Teste) ✅
### Passo 4 (Ferramentas MCP em TypeScript) ✅
- 12 ferramentas implementadas (todas)

### Passo 5 (Dados Populacionais) ✅
- [x] Script R `build-population.R` criado e funcional (desde 2026-09-08 vive no healthbr-data, `scripts/pipeline/sih-cubos/`; decisão 28)
- [x] `pop_municipios.parquet` gerado (1991-2024, 27 UFs, 6.326.761 registros)
- [x] `pop_uf.parquet` gerado (2000-2025, 27 UFs, 127.764 registros; Projeção 2024)
- [x] `pop_uf_agregado.parquet` gerado (1991-1999, derivado dos municípios)
- [x] Os três publicados no canal `sih/cubos/` e baixados pelo servidor (0.12.0, decisão 28)

### Passo 6 (Ferramentas Finais) ✅
- [x] `get_hospitalization_rates` implementada (taxa por 100 mil hab, anos 2000-2024)
- [x] `compare_icsap_trends` implementada (tendências com taxa, regressão linear)
- [x] Validação: erro se ano < 2000 (sem dados populacionais UF)
- [x] Build sem erros, testes de população passando

---

## Decisões Arquiteturais

1. **Idade simples** nos cubos de internação (inteiro 0-100+)
2. **Faixa etária** nos cubos de população municipal (limitação da fonte)
3. **Idade simples** nas projeções por UF (quando disponível)
4. **Grupos CSAP** como string ("g01"-"g19")
5. **Capítulos CID** como inteiro (1-22)
6. **População incluída no MCP** (não depende de outro MCP)
7. **Cliente DuckDB: `@duckdb/node-api`** ("Node Neo", cliente oficial atual), desde 2026-09-05 — o binding legado `duckdb` pinava `node-gyp ^9` em runtime e arrastava tar/cacache/glob antigos para o lock (60 dos 72 alertas do Dependabot de 09/2026); o Neo tem binário pré-compilado por plataforma e nenhum toolchain nativo na árvore. Só `src/db/duckdb.ts` toca o cliente, pelo funil `query()`, que lê com `getRowObjectsJS()` e converte bigint. Plano e medições: `docs/plan-002-migracao-duckdb-node-api.md`
8. **Ordem das linhas garantida** nas consultas agrupadas (`buildOrderBy` no funil, 2026-09-05): toda coluna de agrupamento não ordenada pelo chamador entra como desempate — sem isso, duas chamadas iguais devolviam rankings diferentes em empate e subconjuntos diferentes com `limit`
9. **Gate do CI: smoke + golden pelo stdio** (`scripts/smoke-stdio.mjs`, `scripts/golden-tools.mjs`) sobre a fixture versionada em `tests/fixtures/sih` — superfície e valores das 12 ferramentas byte a byte contra `baselines/`; não é suíte (ver `campanha:contrato-vitest` no portfolio-monitor)
10. **Origem dos cubos SIH: healthbr-data, não o FTP** (2026-09-05). `scripts/build-aggregations.R` v2 lê as partições `sih/rd/ano=AAAA/mes=MM/uf=XX/` do R2 público do healthbr-data (Parquet 1:1 do `.dbc`, colunas cruas como string, projetando só as 10 colunas que os cubos usam — 2023/RR são 45.354 linhas em ~13 s) em vez de baixar o `.dbc` via `microdatasus::fetch_datasus`. Motivo além do volume: `process_sih()` trocava os códigos de SEXO, COD_IDADE e MORTE por rótulos e convertia DT_INTER para `Date`, e o script comparava com códigos — o cubo antigo saiu com `sex = "I"` em todas as linhas, `age` nulo, `deaths = 0` e `month` só com `-1`/`0`. O cubo novo tem os mesmos n, dias, valor, raça e CSAP, e sexo, idade, óbitos e mês corretos. `year`/`month` vêm de DT_INTER (data da internação). **Desde o build v2.2.0 (2026-09-05) o script lê o espelho pelo pacote healthbR** — `healthbR::sih_status()` para saber quais partições existem e de que `.dbc` vieram (URL, MD5, tamanho, SHA-256 do Parquet, versão e commit do pipeline) e `healthbR::sih_data(source = "r2", lazy = TRUE)` para ler as 10 colunas projetadas — e não tem mais código S3 nem token próprio (viviam duplicados no script e no pacote). Cada partição lida é conferida contra a contagem do manifesto. `retrieved_at` do sidecar passou a ser o `processing_timestamp` do manifesto, igual ao segundo ao `download_date` do rodapé do Parquet que o script lia antes; o campo `download_date` saiu do sidecar (opcional no tipo TS). Verificado regenerando 2023/RR: cubos idênticos, golden e smoke verdes sem regravar baseline.
10a. **Cubo por ANO DE INTERNAÇÃO, janela de 4 meses** (2026-09-05, build v2.1.0, `MESES_SEGUINTES <- 4L`). O cubo do ano Y lê as competências Y-01..Y-12 e (Y+1)-01..04 e descarta internações de outros anos. Decidido nos dados, não em hipótese: `docs/analise-001-janela-competencia.md` mede, em 131,8 milhões de AIH (competências 2016-01 a 2026-06, 27 UFs), que 4 meses fecham 99,7–99,9% das internações do ano (dezembro 99,3–99,7%), que o 5º e o 6º mês somam 0,04 ponto, e que a cauda restante (0,06–0,19% depois de +12) é reapresentação difusa em GO, RJ e SP que nenhuma janela recupera. Consequências: `year` é sempre o ano do arquivo e `month` vai de 1 a 12 sem queda artificial no fim do ano; o cubo de Y FECHA quando (Y+1)-04 é publicada (partições posteriores não o alteram; só reedição dentro da janela justifica rebuild — regra para `sih:cubos-frescor`); janela incompleta fica marcada no sidecar (`window.complete = false`) e no `data_vintage` ("janela INCOMPLETA"). 2023/RR: 16 partições, 59.141 AIH lidas, 10.661 de outros anos descartadas, 48.480 internações de 2023 (antes: 45.354 AIH de competência 2023, com 5.884 de 2022 e sem 9.010 faturadas em 2024). Scripts da evidência: `scripts/lag-competencia.mjs` (DuckDB sobre o R2) e `scripts/lag-competencia-analise.py`.
11. **Sidecar de proveniência ao lado de cada cubo** (`data/sih_provenance_<ano>.json`, versionado; cópia na fixture). Gravado pelo script R a partir do rodapé `healthbr` de cada Parquet e do manifesto público (`https://pub-99d9e1a3f5c542178d04efbddf1bba97.r2.dev/sih/rd/manifest.json`): URL do `.dbc` no FTP, MD5 e tamanho da origem, data de download no espelho, SHA-256 do Parquet, contagens, versão do script, R e arrow. É o registro de SAFRA (spec-001 §5): `retrieved_at` do bloco de proveniência = maior data de download dos `.dbc` usados, não o instante da chamada — por isso o golden pode gravar o bloco. Vigiar deriva (o MS reedita arquivos): comparar `source_hash_md5`/`source_size_bytes` do sidecar com o manifesto do espelho — a checagem existe desde 0.5.0 (decisão 13); o rebuild automático ainda não (`sih:cubos-frescor` (b) no portfolio-monitor).
12. **Bloco de proveniência em toda resposta** (`src/provenance.ts`, contrato `@sbissoli/mcp-provenance` v1, modo concise, namespace `br.sbissoli.sih`). A fonte no topo é o Ministério da Saúde/DATASUS; o healthbr-data aparece em `citation`, `derivation_note` e `license` como redistribuição (CC-BY-4.0), nunca como fonte. Ferramentas de referência citam a Portaria 221/2008 ou a CID-10; `get_hospitalization_rates` leva um segundo bloco do IBGE (SIDRA 7358); as de ICSAP levam SIH + Portaria. O texto da resposta é o próprio `structuredContent` (com `provenance` e `attribution`); não há rodapé em parte separada porque smoke e golden juntam as partes e fazem `JSON.parse`. Os scripts de smoke/golden leem o ano do campo estrutural `years` — o regex sobre o texto inteiro pegaria o `2026` da citação.
13. **Frescor dos cubos frente ao espelho, checado na inicialização sem bloquear** (`src/freshness.ts`, 2026-09-05, 0.5.0 — item (a) de `sih:cubos-frescor`). O manifesto público do healthbr-data tem 10,4 MB, o r2.dev não comprime e o `sync-status.json` da raiz do bucket tem 10,7 MB (todos os datasets; não é versão enxuta) — então a checagem é em dois passos: (1) `GET` com `Range: bytes=0-511` (R2 aceita; o `last_updated` está nos primeiros 256 bytes, ~0,5 s) e, se for igual ao `distributor.manifest_last_updated` de todos os sidecars, os cubos estão em dia; (2) só se o manifesto mudou, baixa o arquivo inteiro (~1 s, 29 MB de heap) e compara partição a partição o que cada cubo usou — MD5/tamanho do `.dbc` diferente = reedição do MS (`reedited`), SHA-256 do Parquet diferente = pipeline regenerou (`reprocessed`), partição sumida (`removed`) — e procura competência da janela publicada depois do build (`new_in_window`, só importa para cubo com `window.complete = false`, porque o cubo de Y fecha quando (Y+1)-04 sai). Timeouts 3 s / 12 s; `fire-and-forget` no `main()`; nova checagem em segundo plano quando a última tem mais de 6 h. Veredito (`current` | `stale` | `unknown` | `pending` | `disabled`) sai em `get_available_years.freshness` e, quando `stale`, entra no `data_vintage` do bloco concise ("[ATRÁS do espelho healthbr-data: N partições reeditadas pelo MS (...)]") e em `notices`; `unknown` só em `notices` (o concise não mostra notices — é a única chave visível, por isso o vintage). `SIH_FRESHNESS_CHECK=off` desliga (smoke e golden usam: o baseline não pode depender do portal); a comparação é testada offline por `npm run freshness:selftest` sobre `tests/fixtures/sih/manifest-excerpt.json` (trecho do manifesto real restrito às 16 partições do sidecar; regravar com `npm run freshness:excerpt`). `npm run freshness` mostra o veredito ao vivo (sai 0/3/2 para current/stale/unknown). Isto só AVISA: rebuild automático e cache de cubos são os itens (b) e (c).
14. **Rebuild automático dos cubos atrás do espelho** (`.github/workflows/rebuild-cubes.yml`, 2026-09-06, 0.6.0 — item (b) de `sih:cubos-frescor`; plano e medições em `docs/plan-003-rebuild-cubos.md`). Gatilho em duas camadas, a barata primeiro: `schedule` terça 06:00 UTC (depois do sync-check de segunda do healthbr-data + ~20 h de manutenção) e `repository_dispatch` `healthbr-sih-updated` que o sync-check do healthbr-data envia no fim da manutenção (PR #2 lá; PAT `SIH_DISPATCH_PAT` fine-grained, só Contents no sih) — o dispatch é latência, não decisão: o job `decide` roda `npm run freshness -- --out` e reconstrói só os cubos `behind` (`force` reconstrói todos; `years`+`ufs` cria cubo novo; `unknown` fica VERMELHO, sem rebuild silencioso). O runner tem R (`r-lib/actions/setup-r` com PPPM) e instala `SidneyBissoli/healthbR` do GitHub — a 0.3.1 do CRAN não tem `sih_status()` — e lê o espelho sem segredo nenhum (as credenciais de leitura do R2 são públicas, vivem no pacote). `scripts/rebuild-cubes.R` é a CLI do builder: anos por argumento, UFs de arquivo do sidecar do ano (escopo explícito, sobrevive ao rebuild), exit 1 se um ano falhar (`build_data()` engole). Três gates, porque o golden do CI é da fixture e não vê `data/`: contagem lida = manifesto (já existia); `scripts/cube-delta.mjs` sidecar novo × anterior (reprova partição perdida sem `removed` no espelho, queda > 1% sem retirada, janela regredida, escopo mudado sem pedir; `npm run cube:delta:selftest` no CI); smoke stdio sobre os cubos novos. Publica release `cubes-AAAAMMDD-HHMM` (cubos + sidecars + delta, `GITHUB_TOKEN`) e commita os sidecars no master — o sidecar é a memória do frescor. `sih/cubos/` no R2 e cache local ficam para o (c). **Medido antes de decidir escopo** (2023, 5 UFs, 517 mil AIH): build 2.2.0 levava 562 s, dos quais 35 s de R2 e ~525 s de `map_int(cid, extrair_capitulo_cid)` + `Vectorize(classificar_csap)` — ano nacional extrapolado ~5,4 h; build 2.2.1 classifica os CIDs distintos e espalha por `match` (`por_valor_unico()`): 45,7 s, cubos idênticos. De quebra: `uf_map[codigo]` devolvia vetor NOMEADO e o arrow gravava os nomes nos metadados do Parquet — o cubo de causas de 2023/RR tinha 598 KB com 658 KB de metadado `r` (comprimido); com `unname()` ficou em 242 KB, conteúdo idêntico (a fixture não foi trocada: os valores são os mesmos). Cubo nacional continua decisão do usuário; o custo agora é conhecido (~25–30 min de runner, dominado pela leitura do R2).
15. **Cubo NACIONAL de 2023 em produção; builder 2.3.0 agrega por UF de arquivo** (2026-09-06, à tarde, decisão do usuário). Dispatch `years=2023 ufs=all`: o 1º run (34038116890) leu as 432 partições em 1,6 min e o runner MORREU na agregação — 13,3 milhões de internações num único data frame não cabem em 16 GB. Builder 2.3.0: `agregar_lote()` processa uma UF de arquivo por vez e `somar_lotes()` soma os agregados nas chaves de grupo; mesmo cubo (colunas inteiras idênticas, `value` igual ao último ulp — medido ≤ 1,5e-11 em 5 UFs; 2023/RR idêntico), pico de memória do maior lote (SP, ~3,5 milhões). 2º run (34038899306) verde: 20 min de build (≈25 s por lote são abertura do dataset no R2), delta relatou a mudança de escopo pedida (RR → 27 UFs), smoke sobre os cubos novos OK, sidecar commitado (ffbf51f), release `cubes-20260906-1444` (causas 50 MB, ICSAP 11 MB, séries 36 KB). Totais: 17.969.863 AIH lidas, 13.324.364 internações de 2023, 587.630 óbitos, R$ 20,67 bi, 6.085.836 linhas de causas, 1.264.234 de ICSAP. A fixture segue 2023/RR (golden e smoke do CI não mudaram). Anos seguintes: um dispatch cada. O push do sidecar pelo GITHUB_TOKEN não dispara o ci.yml (comportamento do GitHub), e não precisa.

16. **Janela nacional completa 2019–2024 em produção** (2026-09-06, 30ª sessão; ordem do usuário dada no fim da 29ª). Três dispatches `ufs=all`, dois anos por run: 2024+2022 (run 34041938255, 31 min 54 s, release `cubes-20260906-1551`, sidecar 165cd3a), 2021+2020 (run 34045123114, build 34 min 38 s, `cubes-20260906-1655`, 3dc0fd3) e 2019 (run 34047810068, build 13 min 55 s, `cubes-20260906-1725`, 2a8de9f). Internações por ano: 2019 12.287.939 · 2020 10.615.141 · 2021 11.654.999 · 2022 12.454.873 · 2023 13.324.364 · 2024 14.128.150 — 74.465.466 no total, 27 UFs e 432 partições cada, `window.complete = true` em todos. Conferência local por ano: soma de `n` em causas e séries = `records_in_cube` do sidecar, linhas = `*_rows`; `npm run freshness` `current` para os seis; smoke stdio sobre `data/` OK; `get_available_years` lista 2019–2024. **Defeito achado e corrigido no caminho** (7f272c9): o `workflow_dispatch` fixa o commit do disparo, e o run de 2019, enfileirado atrás do de 2020+2021, teve o push do sidecar rejeitado porque o master já tinha andado — o passo de commit agora faz fetch + rebase + push (3 tentativas); o run 4 provou o conserto. **2025 não foi construído**: o espelho tem a janela completa (2026-01..04 publicadas) mas `pop_uf.parquet` para em 2024, então as ferramentas de taxa não teriam denominador — decisão do usuário. **2026 não**: janela incompleta no espelho. Cubos locais somam ~370 MB (causas ~50 MB/ano, ICSAP ~11 MB/ano); `data/*.parquet` continua gitignored e vem da release.
17. **2025 em produção; população até o último cubo FECHADO; intervalo de taxas lido do arquivo** (2026-09-06, noite, 30ª sessão; o usuário "não se opôs" ao 2025 e deu o sim para reescrever a regra). Cubo 2025: 14.644.591 internações (o maior ano), 19.487.763 AIH lidas, 27 UFs, janela completa (2026-01..04 no espelho); release `cubes-20260906-2248`, sidecar d165770, builder **2.3.1**. Dois runs com o builder 2.3.0 (34062485932 e 34063289778) foram CANCELADOS pelo runner no meio do cubo ICSAP, sem mensagem — estouro dos 16 GB: o passo rodava com `cubo_causas` (6,5 M linhas) e os 27 lotes de causas ainda na memória; 2024 (14,1 M) passou por margem. 2.3.1 solta o cubo de causas e os lotes antes do ICSAP (d768cd6); build de 2025 em 16,5 min. **População:** a regra "PROIBIDO projeções futuras (após 2024)" virou "até o último ano de cubo FECHADO do SIH" (seção de regras); `build-population.R` ganhou a categoria 49041 (2025) da SIDRA 7358 e `pop_uf.parquet` foi regenerado 2000–2025 (122.364 linhas; 2000–2024 idênticos ao arquivo anterior, conferido por EXCEPT nos dois sentidos). A SIDRA 7358 também serve 2026 (49042) — só entra quando o cubo de 2026 fechar. **Servidor:** `getPopulationYearRange()` (src/db/duckdb.ts) lê min/max de `pop_uf.parquet` uma vez; `get_hospitalization_rates` e `compare_icsap_trends` validam por ele e as descrições deixaram de fixar "2000-2024"; `get_available_years` devolve `population_years`. Baselines regravados só por esse diff. Verificado ao vivo: taxa de 2025 para RR e SP com denominador de 2025 (RR 5.247,79/100 mil). Nota: o CONTEXT dizia "Revisão 2024" para `pop_uf`, mas o script baixa a revisão 2018 (`p/2018`) da tabela 7358 — corrigido o texto acima; migrar para a Projeção 2024 do IBGE é item futuro, não urgente.
18. **Canal público dos cubos + cache local** (2026-09-07, madrugada, 30ª sessão — item (c) de `sih:cubos-frescor`, FECHADO; 0.7.0). Decisões do usuário em 06/09: bucket R2 `healthbr-data`, prefixo **`sih/cubos/`** (não `agregados`: os `sipni/agregados/` são DPNI/CPNI oficiais do DATASUS, redistribuição; os cubos são derivados nossos), domínio próprio **`https://data.sidneybissoli.com/`** ligado pelo usuário ao bucket inteiro (o `r2.dev` segue ativo), token R2 "Object Read & Write" restrito ao bucket criado pelo usuário e gravado como secrets `R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY` do repositório. **Publicação:** `scripts/cubes-manifest.mjs` gera `sih/cubos/manifest.json` (SHA-256 e tamanho por arquivo, resumo do sidecar por ano; `--verify` confere cada cubo contra o sidecar no DuckDB antes de assinar; anos não recalculados vêm do manifesto anterior); `scripts/publish-cubes.sh` sobe pelo AWS CLI no endpoint S3 do R2 (arquivos primeiro, manifesto por último, `AWS_REQUEST_CHECKSUM_CALCULATION=when_required` porque o R2 recusa o CRC32 do CLI ≥ 2.23) e confere o content-length pelo domínio público; `rebuild-cubes.yml` publica depois da release (sem secrets, avisa e não falha); `publish-cubes.yml` (manual) republica a partir da release mais recente de cada ano — carga inicial dos 7 anos feita no run 34068597202 (416 MB; SHA-256 de um cubo conferido byte a byte pelo domínio). **Consumo:** `src/cache.ts` — o pacote npm NÃO embarca cubo; sem `sih_*.parquet` em `data/`, `getDataDirectory()` passa a usar `~/.cache/sih-br-mcp/cubos/` (`SIH_CACHE_DIR`), e o handler de ferramentas chama `ensureYears()` antes da consulta com os anos lidos dos argumentos (`year`, `years`, `start_year..end_year`; sem ano = série publicada inteira); cada arquivo é baixado para `.part`, conferido por tamanho e SHA-256 do manifesto e só então renomeado; downloads do mesmo ano não se duplicam (mapa em voo); manifesto memoizado 10 min com cópia em disco para quando a rede falha. `get_available_years` ganha `cubes_channel` (anos publicados, origem do manifesto, pasta do cache). `SIH_CUBES_CACHE=off` desliga (smoke e golden usam). Provado: ponta a ponta com `data/` vazio → 2020/RR respondeu em 6,2 s incluindo 51 MB de download; `npm run cache:selftest` (CI) prova offline, com servidor HTTP local, download íntegro aceito, adulterado de mesmo tamanho recusado pelo SHA-256 sem resto no cache, ano fora do canal em `unavailable`, manifesto do disco sem rede. Fora do escopo: apagar/atualizar cubo do cache (o frescor avisa; repor é apagar e chamar de novo) e Cache Rule da Cloudflare para o domínio (hoje `cf-cache-status: DYNAMIC`).
19. **Série histórica 2008–2018 em produção — 18 anos (2008–2025) no canal** (2026-09-07, madrugada, 31ª sessão; ordem do usuário dada no fim da 30ª: primeira das três etapas de "a maior série histórica de SIH no Brasil"; seguem 1998–2007 e o ESTUDO de 1992–1997, só quando ele nomear). Seis dispatches `ufs=all` do rebuild-cubes.yml, dois anos por run, do mais novo ao mais velho, um rodando + um pendente: 2018+2017 (run 34072157623, build 25 min 05 s, release `cubes-20260907-0135`, sidecar eb6aafd), 2016+2015 (34072242152, 25 min 26 s, `cubes-20260907-0201`, e1fbefa), 2014+2013 (34073602482, 21 min 59 s, `cubes-20260907-0223`, 7e7d09f), 2012+2011 (34074925121, 25 min 13 s, `cubes-20260907-0249`, 4bf7631), 2010+2009 (34076243225, 22 min 52 s, `cubes-20260907-0312`, aef9c3c), 2008 (34077618579, 11 min 23 s, `cubes-20260907-0324`, 55833cc) — 2 h 12 min de build no total, builder 2.3.1 sem nenhuma alteração (2008+ tem todas as colunas que o builder usa; conferido em 06/09 lendo partições de SP). Internações por ano: 2008 11.286.218 · 2009 11.337.490 · 2010 11.559.361 · 2011 11.462.401 · 2012 11.286.700 · 2013 11.350.351 · 2014 11.502.252 · 2015 11.450.030 · 2016 11.367.393 · 2017 11.627.877 · 2018 11.953.006 — 126.183.079 no lote (169.392.479 AIH lidas), 215.293.136 internações nos 18 anos; 27 UFs e 432 partições em cada ano, `window.complete = true` em todos. Canal `sih/cubos/`: 18 anos, 1.001 MB (48,6 MB em 2008 a 57,7 MB em 2018; cada run publicou na hora). Conferência por ano ao chegar: `cubes-manifest.mjs --verify` sobre a release baixada (soma de `n` = `records_in_cube`, linhas = sidecar), `content-length` no domínio = tamanho local, manifesto com os anos novos; ao fim: smoke stdio sobre `data/` com 18 cubos OK, `npm run freshness` `current` para os 18, `get_available_years` 2008–2025 com `population_years` 2000–2025, taxa bruta de 2008 pelo stdio com denominador de 2008 (RR 5.961,37/100 mil; SP 5.690,96). **Defeito achado e corrigido antes do 1º run** (fb1b84c): a 0.7.0 fez `getDataDirectory()` devolver o cache local em vez de lançar erro quando `data/` não tem Parquet, e `loadSidecars()` usava esse erro como critério para ler os sidecars versionados — num checkout limpo o job `decide` não achava sidecar nenhum e morria com ENOENT em `freshness.json` (run 34071934742, 19 s); teria derrubado também o schedule de terça. Três camadas: a pasta configurada tem precedência sempre que tiver sidecar (o cache só vale sem nenhum); `freshness-check.mjs --out` grava `unknown` com o motivo mesmo quando a checagem não roda; o passo `decide` cria o estado `unknown` se o arquivo faltar (pedido à mão segue, schedule para com veredito). Reproduzido localmente com `--fixtures` numa pasta só com sidecars. Nota para 1998–2007: sem `RACA_COR` (raça vazia — decidir como o cubo e as notas das ferramentas declaram); taxas de 2000 em diante.

20. **Série histórica 1998–2007 em produção — 28 anos (1998–2025) no canal; raça/cor nula antes de 2008** (2026-09-07, 32ª sessão; ordem do usuário dada no fim da 31ª: segunda das três etapas da série; resta o ESTUDO de 1992–1997, que não é build). **Duas mudanças de código antes do 1º run**, porque o lote não era dispatch puro: (a) healthbR 0.4.0.9000 (2a2840c, main): `sih_years()` começava em 2008 mesmo com `source = "r2"` — o espelho tem 1992–2026, o limite era o da pasta FTP `SIHSUS/200801_`; agora 1992–2024 pelo espelho, FTP segue 2008+ com mensagem que aponta o R2; a 0.4.0 tinha entrado no CRAN nesse mesmo dia (tag v0.4.0 + release em 4b5e5fb), então a mudança foi para o main e o rebuild-cubes.yml segue instalando `SidneyBissoli/healthbR` (o `any::healthbR` do CRAN limitaria o SIH a 2008+); (b) builder 2.4.0 (6af2167): `RACA_COR` vira OPCIONAL (`COLUNAS_OBRIGATORIAS`/`COLUNAS_OPCIONAIS`); `ler_particoes()` lê só as presentes e conta como AUSENTE no ano a coluna que nenhuma linha da competência do ano traz — o healthbR impõe ao prefixo o esquema do ano mais novo pedido, então o cubo de 1998 (lê 1998+1999) não vê a coluna e o de 2007 (lê 2007+2008) a vê com NA, e os dois caminhos dão o mesmo cubo; **decisão do usuário (07/09): `race` NULO em todas as linhas** desses cubos (não "ignorado", não valor próprio; nem nas internações faturadas em 2008-01..04 que já trazem a coluna), sidecar com `columns_missing: ["RACA_COR"]` + nota; `build_data()` recusa ano que o healthbR instalado não aceita; o workflow exige `1998 %in% sih_years()` no passo de conferência. Servidor 0.8.0: `get_available_years` devolve `race_available` por ano e `years_without_race`; get_hospitalizations, get_icsap e get_icsap_indicators acrescentam `notes` quando filtro ou agrupamento por raça alcança ano sem a coluna (o grupo sai `null` no JSON, sem rótulo inventado); descrições avisam. Prova local antes do push: 2007/RR e 1998/RR com raça nula e `columns_missing`; 2023/RR IDÊNTICO à fixture (0 linhas de diferença nos 3 cubos, EXCEPT ALL nos dois sentidos) — golden regravado só pelos campos novos e pela versão. **Seis dispatches** `ufs=all`, 2007 sozinho primeiro (prova do caminho) e depois pares, um rodando + um pendente: 2007 (run 34120365747, build 10 min 49 s, release `cubes-20260907-1220`, sidecar 9e912e9), 2006+2005 (34121713950, 22 min 04 s, `-1246`, 88b659e), 2004+2003 (34121743113, 19 min 31 s, `-1306`, 860ac34), 2002+2001 (34123919366, 20 min 44 s, `-1327`, ee4a4d5), 2000+1999 (34125653855, 17 min 40 s, `-1345`, 9e27671), 1998 (34127732960, 10 min 25 s, `-1356`, af0aa1b) — 1 h 41 min de build. Internações por ano: 1998 12.267.804 · 1999 12.427.470 · 2000 12.493.736 · 2001 12.326.426 · 2002 11.987.201 · 2003 12.104.159 · 2004 11.676.502 · 2005 11.487.429 · 2006 11.764.406 · 2007 11.260.764 — 119.795.897 no lote (160.244.343 AIH lidas), **335.089.033 internações nos 28 anos**; raça nula nos 10 (conferido no DuckDB sobre os cubos baixados da release). **Lacunas do próprio FTP do DATASUS** (conferidas na listagem de `SIHSUS/199201_200712/Dados/`, o espelho é fiel): RDAP0710 (Amapá, out/2007) e RDRR9912–RDRR0005 (Roraima, dez/1999 a mai/2000) nunca foram publicados → cubos 2007 (431 partições), 1999 e 2000 (427) saem com `window.complete = false`; a taxa de RR em 2000 fica subestimada (3.199/100 mil contra 6.329 em SP) e é assim que a fonte é. Canal `sih/cubos/`: 28 anos, 112 arquivos, 1.307 MB. Conferência final: smoke stdio sobre `data/` com 28 cubos OK, `npm run freshness` `current` nos 28 (via range), `get_available_years` 1998–2025 com `race_available` falso em 1998–2007 e `population_years` 2000–2025, taxa de 2000 OK, taxa de 1998 recusada com a mensagem do intervalo, `group_by: ["race"]` em 2005 devolve o grupo null com a nota. Próximo: estudo 1992–1997 (codificação numérica de 6 dígitos em `DIAG_PRINC`; 1992 sem `MUNIC_RES`) — pesquisa, não build.
21. **Estudo 1992–1997 (era CID-9) CONCLUÍDO — pesquisa, não build; decisões do usuário pendentes** (2026-09-07, 33ª sessão; ordem do usuário dada no fim da 32ª: terceira etapa de "a maior série histórica do SIH"). Documento: `docs/analise-002-sih-1992-1997.md`; evidência em `scripts/estudo-1992-1997*.{mjs,py}` (DuckDB httpfs sobre as 1.932 partições de 1992–1997 do R2, 84.627.338 AIH, e as tabelas `TAB_SIH_199201-199712.zip` do DATASUS). **Fatos:** `DIAG_PRINC` é CID-9 da OMS em 6 dígitos — prefixo (0 categorias, 1 causas externas E, 2 suplementar V) + categoria + subcategoria + dígito verificador (módulo 11, pesos 6,5,4,3,2; provado em 8.238/8.240 códigos das `CID9_*.CNV`); 100 % das AIH têm 6 dígitos, 0 vazios, 3 com DV errado, 0 fora das tabelas do DATASUS — decodificar é tabela versionada (o DATASUS tem duas exceções ao DV, `080120`/`080781`, e os dados as seguem). `DT_INTER` (`YYMMDD`) 100 % vazio nas competências 1992-01..04 e 1993-01 (5,08 M + 1,26 M AIH, 27 UFs) e 0 % no resto; `MUNIC_RES` inexistente em 1992–93, 100 % vazio em 1994-01..11, 7–10 % vazio em 1995–97 (TabNet: residência coletada só de 1995); sem `RACA_COR`; `COD_IDADE = 0` e `SEXO = 0` em 4–5 %; `VAL_TOT` na moeda da época (Cr$ → CR$ jul/1993 → R$ jul/1994); janela de 4 meses vale igual (99,74–99,85 % em +4); **fronteira 1997/1998 mista**: 685.870 internações de 1997 faturadas em 1998-01..04 já vêm em CID-10 e `YYYYMMDD` (o cubo de 1997 precisa de `cid_revision` por linha). ICSAP: nenhuma lista oficial em CID-9 (Alfradique 2009 converteu CID-9→CID-10 sem publicar a tabela; AHRQ PQI v6.0/2016 e Caminal 2004 são ICD-9-CM; CMS GEMs 2018 domínio público, aproximadas; OMS só tem tabela do capítulo V). Mapa de capítulos CID-9 → CID-10 por faixa de categoria com 3 exceções (135, 279, 446; 0,15 % das AIH) reproduz os 10 grupos do IDB do DATASUS sem exceção. **Recomendação:** construir 1992–1997 (builder 2.5.0 "era antiga", item próprio, ~2 h de runner) com `cid_chapter` pelo mapa, `cid_group` = categoria CID-9 + coluna `cid_revision`, raça NULL, ICSAP NULL, `uf` = UF do arquivo na era, `value` nominal com `currency` no sidecar, AIH sem data atribuídas à competência com `records_date_imputed`. Seis decisões do usuário, uma por mensagem (§5 do documento). **Defeito colateral achado e NÃO corrigido (ordem: só estudo):** `build-aggregations.R` inverte `COD_IDADE` (trata 2 como meses e 3 como dias; a fonte e o dicionário do healthbR dizem 2 = dias, 3 = meses) — recém-nascidos de 12–30 dias saem com `age` 1 ou 2 em todos os 28 cubos (SP 2023: 6.231 AIH, 0,23 %; 8 % dos neonatos em dias); correção = 1 linha + teste de contrato + rebuild dos 28 anos (~6 h de runner); registrado no TAREFAS.md do monitor como `sih:cod-idade`. **Decisões do usuário (07/09, fim da 33ª, uma por mensagem):** (i) construir os seis anos 1992–1997; (ii) AIH sem `DT_INTER` (1992-01..04 e 1993-01) atribuídas à competência, `records_date_imputed` no sidecar e nota nas ferramentas; (iii) ICSAP com **lista CID-9 derivada, "a mais robusta possível", ANTES do build** — por grupo da Portaria 221, códigos das listas de origem em CID-9 (Caminal 2004, Billings 1993, AHRQ PQI v6.0, CIHI), GEMs só no resíduo e marcadas, validação pelos pares do DATASUS e validação empírica na fronteira 1997/98 (internações de 1997 em CID-9 vs em CID-10, razão de comparabilidade por grupo e UF), tabela versionada com fonte/método/licença/razões, `icsap_list_revision = "cid9-derivada"`, nota nas ferramentas; (iv) `uf` = UF do arquivo nos seis anos, município nulo, **muito bem documentado** (sidecar `uf_basis`, descrições, `get_available_years`, docs do canal); (v) `value` nominal na moeda da época com `currency` por período e nota; taxas 1992–1999 via `pop_uf_agregado` viraram item próprio no monitor (`sih:taxas-1992-1999`); (vi) cubo de 1997 com a janela normal e `cid_revision` por linha (ausente = 10 nos cubos 1998+). Ordem: lista ICSAP CID-9 (item próprio) → builder 2.5.0 + correção `sih:cod-idade` numa campanha só → taxas.
22. **Lista ICSAP em CID-9 derivada e validada — `src/data/csap-groups-cid9.json`; builder não mudou** (2026-09-07, 34ª sessão; ordem do usuário dada no fim da 33ª: executar a decisão (iii) do estudo 1992–1997 "de forma eficiente e correta"). Documento: `docs/analise-003-icsap-cid9.md`; scripts `estudo-icsap-cid9-derivar.py` (a correspondência em código, reprodutível das tabelas `CID9_*.CNV` do DATASUS; falha se rubrica inexistente ou em dois grupos), `estudo-icsap-cid9-fronteira.mjs` (recortes A/B/C no healthbr-data) e `estudo-icsap-cid9-analise.py` (razões, classes, `validation.json`). **Lista:** 19 grupos, 156 rubricas CID-9 (OMS), 439 códigos de 6 dígitos, cada rubrica com `source[]`, `method` (exact/category/approximate), rótulo do DATASUS, AIH de 1997 e nota; `not_official: true`, `icsap_list_revision: "cid9-derivada"`. Correspondência MANUAL por rubrica homônima (todas), com Caminal 2004 (núcleo/ampliada/revisados — 72 rubricas), AHRQ PQI v6.0 ICD-9-CM 2016 (31), CIHI 2008 coluna ICD-9 (22), par `DIAGPACTO9` do DATASUS (250.1–250.2) e GEMs só em 514 e na conferência cruzada; perímetro da Portaria manda (485/486, 482.4, 483, 558, 590.2, 430/431, 410 ficam fora mesmo constando nas listas de origem — `metadata.excluded` com o peso de 1997). Cobre 26,16 % das AIH de 1997. **Validação empírica** (participação por grupo: 1997 em CID-9 vs 1998-03..12 em CID-10, classes a priori alta 0,80–1,25 / média 0,67–1,50 / baixa): razão global **1,05**; 16 grupos alta, g06 média (0,76), **g03 (0,21) e g05 (4,75) baixa por mudança de prática de codificação** (281.9 → D50.9/D64.9; 465.9 IVAS 73 mil → J06 15 mil), não da lista — ficam marcados `comparability`. A validação decidiu: 411 FORA da angina (↔ I24; razão 1,14 sem, 1,44 com) e 8 rubricas asterisco específicas dentro (320.4, 484.0, 484.3, 730.4–6, 362.0, 357.2; 614 AIH). Jan–fev/1998 têm ICSAP total em 19–20 % (adaptação à CID-10) e voltam a 24,5 % em março — as internações de 1997 faturadas nesses meses terão ICSAP subestimado (efeito da fonte). **Armadilhas registradas:** ICD-9-CM renumera 250.x (CM 250.4 renal = OMS 250.3), 320.4 é meningite tuberculosa na OMS, 411 sem subdivisão na OMS, 481 inclui a lobar NE (J18.1), AHRQ/CIHI excluem 401.1 e a Portaria não. Recortes A (mesmas internações de dez/1997 nos dois códigos) e B (competências vizinhas) rodados pelo usuário em seguida: confirmam g03 e g05 como únicos desvios e mostram que a queda de jan–fev/1998 foi desigual por UF (BA 28 → 9,5 %, SC, GO, MA, RJ, PB > 1,8; PE, SE, TO, ES, AL, PA, AM ≈ 1) — o ICSAP das internações de 1997 faturadas em 1998-01..04 será subestimado nessas UFs (analise-003 §3.4). **Para o builder 2.5.0:** classificar por tabela (`codes6` → grupo) quando `cid_revision = 9`, sidecar com `icsap_list_revision` e `icsap_comparability`, nota nas ferramentas para ano < 1998; unificar a lista CID-10 em tabela também.
23. **Builder 2.5.0 "era antiga" 1992–1997 + correção `sih:cod-idade` + denominador do % ICSAP — servidor 0.9.0** (2026-09-07, 35ª sessão; ordem do usuário dada no fim da 34ª: executar a campanha "de forma eficiente e correta", sem cardápio). **Código provado localmente antes de qualquer dispatch.** Builder (`scripts/build-aggregations.R` 2.5.0): `FIRST_YEAR 1992`; `MUNIC_RES` vira OPCIONAL (mesmo mecanismo de `RACA_COR`); `DT_INTER` lido pelo comprimento (6 = `AAMMDD` até 1997, 8 = `AAAAMMDD`), VAZIO → dia 1 da competência do arquivo, contado em `records_date_imputed` DEPOIS do filtro de ano (as AIH sem data de 1993-01 lidas na janela de 1992 vão para o cubo de 1993); `cid_revision` por linha (9 se `DIAG_PRINC` tem 6 dígitos, 10 caso contrário) como CHAVE nos três cubos em todos os anos; CID-9 decodificada por TABELA — `src/data/cid9-codes.json` (8.240 códigos das `CID9_01..17` + `CID9_SUP.CNV` do DATASUS, rubrica, categoria, rótulo, capítulo CID-10 equivalente, válido) e `src/data/cid9-chapters.json` (faixas + 3 exceções), gerados por `scripts/cid9-tables.py` a partir do CSV do `estudo-1992-1997-cnv.py` (`scripts/tab/` e os CSV ficaram gitignored); `cid_group` em CID-9 = categoria de 3 dígitos, `cid_chapter` pelo mapa; código fora da tabela (os da `CID9XINV.CNV`, que o DATASUS marca inválidos — 158 AIH em 1992/SE, ~0,1 %) sai com grupo e capítulo nulos; ICSAP por tabela nas DUAS revisões — `csap-groups-cid9.json` (`codes6` → grupo) e `csap-groups.json` (CID-10), esta **provada idêntica a `classificar_csap()` nos 11.757 códigos das `CID10_*.CNV` do DATASUS (0 divergências; a função ficou como referência da prova `provar_csap_cid10_tabela()`)**; `uf` = UF DO ARQUIVO e `municipality_code` nulo até 1997 (`UF_ARQUIVO_ATE_ANO`; regra por ANO do cubo, não por presença da coluna, para que os seis anos tenham um eixo só); SEXO "2" → F; **cod-idade corrigido** (`COD_IDADE` 2 = dias e 3 = meses, ambos 0 anos); sidecar `manifest_version 1.1.0` com `records_date_imputed`, `cid_revision` (internações por revisão), `icsap_list_revision` por revisão, `not_official_icsap`, `icsap_comparability` (só com CID-9), `uf_basis`, `municipality_available`, `currency` (por competência da janela: BRE Cr$ até 1993-06, BRR CR$ 1993-07..1994-06, BRL R$ desde 1994-07 — sempre presente, constante BRL em 1995+), notas novas por condição. **Defeito achado ao provar ponta a ponta e corrigido na camada certa: o % ICSAP do servidor estava errado desde a primeira versão do cubo** — o cubo ICSAP tinha uma linha por (estrato × grupo CSAP) com `n_total` do estrato repetido em cada uma, e só os estratos com alguma ICSAP; o servidor somava `n_total` linha a linha (denominador multiplicado pelo número de grupos e sem os estratos sem ICSAP): a fixture 2023/RR dava `icsap_percentage` **3,65 %** quando 9.577/48.480 = **19,75 %** (o golden tinha fossilizado o erro). Correção: o builder grava TODOS os estratos (linha com `csap_group` nulo e `n = 0` para estrato sem ICSAP; 2023/RR: 6.014 linhas, 1.940 nulas; sidecar `totals.icsap_rows_without_csap`) e o servidor calcula o denominador em `icsapAggregateSql()` (`src/db/duckdb.ts`): `n_total` somado sobre estratos DISTINTOS (`year, uf, municipality_code, cid_revision, sex, age, race`), numerador só das linhas com grupo, filtro por grupo CSAP só no numerador, `total LEFT JOIN icsap` para um UF/ano sem ICSAP aparecer com 0; `queryIcsap`, `calculateIcsapIndicators` e o resumo de `get_icsap` (consulta sem agrupamento em vez de somar as linhas) passam por ele; `rank_csap_groups` ignora o grupo nulo. De quebra: `SUM(value)` virou `SUM(CAST(value AS DECIMAL(18,2)))` em todas as consultas — o SUM paralelo de doubles do DuckDB mudava o último dígito entre execuções (golden reprovou contra si mesmo: 9746572.610000005 vs …01); e `read_parquet(..., union_by_name = true)` + `COALESCE(cid_revision, 10)` nos agrupamentos, defesa para cache local misto. **Servidor 0.9.0** (`src/provenance.ts`, `src/index.ts`): tipo `SihSidecar` com os campos novos; `yearsCid9()`, `yearsUfArquivo()`, `yearsPreReal()`, `dateImputedByYear()`, `eraNotes(years, loaded, aspects)` (uf do arquivo e CID-9 sempre que um ano da era entra; ICSAP derivada, moeda, município nulo e mês imputado só quando a ferramenta usa a dimensão — vazio para 1998+, o golden da fixture 2023 não muda); `csapCid9Provenance()` (derivada, não oficial, `derivation_note` exigido pelo contrato) entra como terceira fonte nas quatro ferramentas ICSAP quando a consulta alcança ano em CID-9; `get_available_years` ganha por ano `cid_revision`, `icsap_available`, `icsap_list_revision`, `uf_basis`, `municipality_available`, `currency`, `records_date_imputed` e as listas `years_cid9`/`years_uf_arquivo`; `group_by` aceita `cid_revision` em get_hospitalizations, get_icsap e get_icsap_indicators; `notes` em get_hospitalizations, get_hospitalization_trends (mensal e anual), compare_regions, get_icsap, get_icsap_indicators, rank_csap_groups e compare_icsap_trends; **`compare_icsap_trends` só valida o intervalo de população para `rate_per_10k`** (barrava 1998–1999 em `percentage`/`count` sem motivo); descrições dizem onde a era muda; `classify_as_csap` segue CID-10 e diz isso. **Provas locais** (`--out` no scratchpad): (a) 2023/RR vs fixture — `n`, `days`, `value`, `deaths` idênticos nos três cubos; sem `age` e com `value` a 2 casas, 0 linhas diferentes; exatamente **358 AIH** passaram de `age` 1–2 para 0 = as com `COD_IDADE = 2` e `IDADE >= 12` no lote cru (contrato "código 2 ou 3 nunca dá idade > 0" verificado no lote); (b) 1997/SE — 126.685 internações em CID-9 e 3.879 em CID-10 no mesmo cubo, ICSAP 19,7 % / 18,8 %, `uf` = SE, município nulo, capítulos 1–22; (c) 1992/SE — 141.616 internações, 43.030 com data imputada (as 54.867 vazias da janela menos as de 1993-01), moeda BRE, `columns_missing` MUNIC_RES + RACA_COR. Fixture 2023/RR regerada (README da pasta), baselines de smoke e golden regravados (golden passa duas vezes seguidas), `cache/freshness/cube:delta` selftests OK, `cubes-manifest --verify` OK em 1992 e 1997 (em 2023/RR reclama "8 UFs no cubo != 1 no sidecar": a verificação compara UF de residência com UF de arquivo e só vale para cubo nacional ou de era — limitação pré-existente, não afeta produção). Ponta a ponta pelo stdio sobre 1992 + 1997 + 2023: campos novos em `get_available_years`, notas certas por ferramenta, `compare_icsap_trends` 1992→2023 em `percentage` funciona e recusa `rate_per_10k` fora de 2000–2024. Workflow: checagem `1998L` → `1992L`. Golden ganhou dois casos com `cid_revision`. **Primeiro run da era (1992+1993, `ufs=all`): verde em 21 min (run 34162346804, release publicada com o builder 2.5.0)** — o caminho CID-9 aguenta a escala nacional; o run 1994+1995 foi cancelado porque o item seguinte muda o cubo e todos os anos são reconstruídos de novo.
24. **% ICSAP pelo método do csapAIH — builder 2.6.0, servidor 0.10.0** (2026-09-07, 35ª sessão; ordem do usuário durante a campanha: "para calcular o ICSAP corretamente, basear-se no pacote R csapAIH, de Fúlvio Nedel, a metodologia consagrada na literatura brasileira"; confirmado que o arranjo adota as três exclusões do pacote como universo padrão). **O que o csapAIH é, lido no código (v0.0.4.8, 2026-01-16, GPL-3):** a lista de diagnósticos (`listaBRAlfradique`) é idêntica à tabela deste projeto código por código; a `listaBRMS` do pacote tem três desvios de regex contra a própria Portaria (inclui `B5[0-6]` = B55 leishmaniose e B56 Chagas, omite B05 sarampo e B06 rubéola, e só casa `B77 ` com espaço) — o pacote documenta que as duas listas não diferem em diagnósticos, logo são erros, não método; **este servidor segue a Portaria** (medido em 2023/RR: 25 AIH de B55.x que o csapAIH classifica em g01 e 4 de B77.x que ele perde). O que muda de verdade é o UNIVERSO: `csapAIH()` exclui, antes de classificar, as internações por procedimento obstétrico (`proc.obst()`, 10 códigos SIGTAP), as com diagnóstico de parto (`partos()`, O80–O84) e as AIH de longa permanência (`IDENT = 5`), do numerador E do denominador. **Builder 2.6.0:** `PROC_REA` e `IDENT` entram nas colunas obrigatórias (existem em todas as eras: 8 dígitos da tabela antiga até 2007, 10 dígitos SIGTAP desde 2008); `src/data/csap-universe.json` (gerado por `scripts/csap-universe-tables.py`) traz os 10 códigos SIGTAP do csapAIH ao pé da letra, os 22 códigos dos grupos partos normais/cesáreos/abortamentos de `PROCOBST.CNV` do DATASUS para a tabela antiga (conferido em 2005/SE: os partos O80–O84 têm PROC_REA 35001011, 35025018, 35009012, 35026014, todos na lista), parto em CID-10 O80–O84 e em CID-9 650 e 669.5–669.7 (10 códigos de 6 dígitos; 651 é gestação múltipla e não conta), IDENT 5; `derivar_exclusao()` marca cada linha (precedência procedimento_obstetrico > parto > longa_permanencia) e `exclusion` é CHAVE nos cubos de causas e ICSAP (e do estrato do n_total) SEM apagar a classificação CSAP; sidecar `csap_universe` (método, fonte, regras, `records_in_universe`, `excluded` por motivo) e nota. **Servidor 0.10.0:** parâmetro `universe` (`csapaih` padrão | `all`) em get_icsap, get_icsap_indicators, rank_csap_groups e compare_icsap_trends — `icsapAggregateSql` aplica `exclusion IS NULL` ao numerador e aos estratos do denominador; `exclusion` entrou nas chaves do estrato (sem isso dois estratos que só diferem no motivo colapsavam no DISTINCT: 48.122 em vez de 48.480 com `all`); `rank_csap_groups` idem; nota de universo em toda resposta ICSAP; `csapAihProvenance()` (derivada, GPL-3, com a divergência de regex registrada no `derivation_note`) como quarta fonte das ferramentas ICSAP; `get_hospitalizations` agrupa por `exclusion`; `get_available_years.csap_universe` por ano. **Prova cruzada com o próprio pacote (2023/RR, 48.480 AIH cruas lidas do espelho e passadas a `csapAIH::csapAIH()` com os padrões):** mesmo universo, 38.020 internações (10.460 fora: 9.766 por procedimento + 694 por diagnóstico de parto; RR 2023 não tem AIH de longa permanência); ICSAP 9.577 no cubo vs 9.595 no pacote, 18 dos 19 grupos idênticos, g01 584 vs 602 = exatamente as B55.x; % ICSAP 25,19 % (pacote 25,24 %) contra 19,75 % com todas as internações. 1997/SE: 44.259 por procedimento obstétrico (34 %), 2.622 de longa permanência, ICSAP 30,7 % no universo; 1992/SE 31,9 %. Fixture regerada, baselines regravados (golden estável em duas execuções), bateria OK. Os 1992+1993 publicados pelo run anterior (2.5.0) são substituídos no rebuild geral. Os três desvios da regex foram relatados ao autor a pedido do usuário: **fulvionedel/csapAIH#17** (07/09/2026), com a evidência de 2023/RR e a regex corrigida sugerida. **Dispatches (concluídos em 08/09/2026, 36ª sessão):** era com o 2.6.0 em 3 runs (34165675825 = 1992+1993, 34165701182 = 1994+1995, 34166940586 = 1996+1997; 22:09–23:11 UTC de 07/09) e rebuild dos 28 anos em 14 runs de 2 anos, do 2025+2024 ao 1999+1998 (22:52 UTC de 07/09 → 05:40 UTC de 08/09, 20–35 min cada, fila automática de 1 rodando + 1 pendente; gates de manifesto, delta e smoke verdes nos 17). Canal conferido ao fim: 34 anos, 420.103.883 internações, 1.577 MB; `manifest.json` (05:34 UTC) igual aos 34 sidecars do master em `built_at` e `records_in_cube`; `npm run freshness` sem atraso nos 34; 1997 misto (11.734.358 CID-9 + 685.870 CID-10); universo do csapAIH de 11,7 M (1992) a 12,7 M (2025) internações. Armadilha: `cubes-manifest --verify` LOCAL reprova 1998 porque os parquet em `data/` são de builds locais antigos (gitignored) — o verify que vale é o do runner, que assina o manifesto. Branch `taxas-1992-1999` mesclada só depois da fila (3e60a99, CI verde).
25. **Taxas por 100 mil para 1992–1999 (`sih:taxas-1992-1999`) — servidor 0.10.0, branch `taxas-1992-1999` feita enquanto a fila de rebuild rodava** (2026-09-07, 35ª sessão; escolha do usuário no cardápio; mesclar no master só depois da fila, porque cada dispatch constrói com o master do momento). O denominador já existia (`pop_uf_agregado.parquet`, 1991–1999, por UF, sexo e faixa etária quinquenal, somado dos municípios via `csapAIH::ler_popbr`) e `getPopulation()` já caía nele — só a validação e as descrições barravam. **Servidor:** `getPopulationCoverage()` lê a cobertura dos DOIS arquivos (união = o que as taxas aceitam; `detailed` 2000+ idade simples, `aggregated` 1991–1999 com a lista das faixas) e substitui o intervalo fixo em `pop_uf`; `populationSourceFor(ano)`; `aggregatedAgeGroupsFor(age_min, age_max)` — antes de 2000 o recorte etário só vale nos limites das faixas (age_min múltiplo de 5, age_max terminado em 4 ou 9, ou 80+): intervalo fora dos limites é ERRO com a lista das faixas, não aproximação (antes o filtro de idade era ignorado em silêncio para ano < 2000 e ainda caía no fallback dos municípios, sem idade); `get_hospitalization_rates` valida pela cobertura, devolve `population_source` por linha e `population_notes` (qual arquivo serviu; aviso quando a consulta mistura os dois); `compare_icsap_trends` idem para `rate_per_10k`; `get_available_years.population_years` vira o objeto de cobertura. **Achado nos dados:** o POPBR de 1993–1999 traz linhas com `fxetaria = "I000"` (idade ignorada; ~0,3 % da população, em 1993 com o mesmo valor para os dois sexos) que `ler_popbr()` deixa com `fxetar5` NA e o parquet grava com `age_group` nulo — o total do Brasil de 1993 só bate com a estimativa do IBGE (151.556.521) COM essas linhas, então elas ficam no total e saem de qualquer recorte etário (`queryPopulationUfAgregado` só filtra `age_group` quando há faixas). **Prova (cubos de prova 1992/SE e 1997/SE, denominadores conferidos à mão no parquet):** 1992/SE bruta 141.616 / 1.515.856 = 9.342,31 por 100 mil; 0–4 anos 19.654 / 190.592 = 10.312,08; feminino 89.370 / 773.650 = 11.551,74; 80+ 3.086 / 14.970; 1997/SE 130.564 / 1.657.162 = 7.878,77; 0–17 recusado com a lista das faixas; 1985 e 1989 recusados pela cobertura; 1997+2023 com as duas notas; `compare_icsap_trends` 1992→2023 em `rate_per_10k` funciona. Golden ganhou dois casos (1995 com 0–17 recusado; 1995/RR 0–4 sem cubo). Ressalva documentada nas notas: antes de 1998 `uf` do cubo é a do arquivo e a população é de residência.
26. **`sih:canal-acabamento` itens 2 e 3 — domínio próprio e Projeção 2024 (branch `taxas-1992-1999`, builder 2.6.1, servidor 0.10.0)** (2026-09-07, 35ª sessão; escolha do usuário no cardápio enquanto a fila de rebuild rodava). **(2) Domínio próprio no lugar do `r2.dev`, com o `r2.dev` de reserva:** `src/freshness.ts` ganha `preferDomain()` — toda leitura do espelho tenta `https://data.sidneybissoli.com` (mesmos caminhos; `HEALTHBR_DATA_DOMAIN` sobrescreve) e cai na URL original do sidecar se o domínio falhar; o builder grava `distributor.manifest_url` no domínio (2.6.1; os sidecars antigos continuam válidos pelo `preferDomain`); healthbR (`R/utils-r2.R`): `healthbr_r2_pub_domain` + `.r2_pub_urls()` — `.r2_manifest()` faz HEAD no domínio e depois no `r2.dev`, baixando de quem respondeu (`options(healthbR.r2_pub_base = ...)` sobrescreve; `sipni_info()$url_r2` aponta para o domínio; NEWS). Provado com o domínio bloqueado por option inválida: cai no `r2.dev`. **(3) Projeção 2024 do IBGE no `pop_uf.parquet`:** a SIDRA 7358 só tem a revisão 2018 (`p/2018`; a API de períodos confirma) — a Revisão 2024 está apenas na planilha oficial `projecoes_2024_tab1_idade_simples.xlsx` do FTP do IBGE (idade simples 0–90+, sexo, Brasil/regiões/UFs, 2000–2070). `build_pop_uf()` reescrito para a planilha (readxl; `build_pop_uf_sidra2018()` fica como legado): filtra as 27 UFs, Homens/Mulheres, 2000–`POP_UF_ULTIMO_ANO` (2025), confere Homens + Mulheres = Ambos por UF × ano e Brasil 2024 = 212.583.750. Diferença material: a revisão 2018 superestimava — Brasil 2023 221.272.301 → 211.695.158 (−4,3 %), 2025 224.450.188 → 213.421.037; SP 2023 48,5 → 45,9 milhões; RR 2023 688 → 695 mil; toda taxa por 100 mil de 2000–2025 sobe ~4 % (2023/RR fixture: 21,91 → 22,90). O arquivo agora tem a grade completa (127.764 linhas = 27 × 2 × 91 × 26; o antigo omitia linhas com população zero). Fixture `pop_uf.parquet` trocada; golden regravado (estável em duas execuções); bateria OK. **(1) Cache Rule da Cloudflare: FEITA em 07/09 pelo MCP oficial da Cloudflare** (plugin `cloudflare@cloudflare` instalado nesta sessão pelas instruções de developers.cloudflare.com/agent-setup; OAuth autorizado pelo usuário com template de edição — o OAuth do wrangler só tinha leitura). Zona `f5836f8e8fc18cf1eae08c04ed5e6292`, ruleset `3877f1d73af844a2b87a652c3e70f35e` (fase `http_request_cache_settings`), duas regras: `http.host eq "data.sidneybissoli.com"` → elegível, TTL da borda pelo `Cache-Control` da origem (objetos 1 dia); e `... and ends_with(http.request.uri.path, ".json")` → TTL de 300 s na borda e no navegador (a origem manda 300 s, mas a zona reescrevia o navegador para 14.400 s), para um rebuild aparecer em até 5 min. Conferido com GET (HEAD não é cacheado): MISS → HIT no cubo e no manifesto, `Cache-Control: max-age=300` no manifesto. Purga feita nos três manifestos. **Raiz do domínio:** o R2 não tem listagem nem documento-índice, então `https://data.sidneybissoli.com/` devolvia o 404 da Cloudflare (pergunta do usuário); a pedido dele, Worker `healthbr-data-index` (healthbr-data `workers/data-index/`, commit e936067) com rota só na raiz exata (`data.sidneybissoli.com/`), binding R2 `BUCKET`, página que lista os datasets, aponta os manifestos e lê `sih/cubos/manifest.json` para o bloco dos cubos (34 anos, 420 M internações, 1.471 MB); deploy pela API do MCP (multipart com `main_module`), rota criada; conferido: raiz 200 text/html, caminhos do R2 intocados e com HIT, prefixo sem objeto segue 404. No mesmo commit: card `guides/dataset-cards/sih-cubos-README.md` e a linha dos cubos no README (34 anos, 1992–2025). Publicar o card no HF/R2 é `python scripts/maintenance/publish-cards.py sih-cubos` (exige chaves R2; fica para o usuário). **(4) publicar no npm** segue pendente (2FA por chave, terminal do usuário).
27. **Produtor dos cubos migrado para o healthbr-data — servidor 0.11.0 consumidor puro** (2026-09-08, 37ª sessão; ordem única do usuário dada no fim da 36ª; desenho aprovado na 31ª: "healthbr-data é produtor; MCP é consumidor, como o healthbR será"). O que saiu daqui: `rebuild-cubes.yml`, `publish-cubes.yml`, `scripts/build-aggregations.R` (2.6.1 → 2.7.0 lá, mesma agregação), `rebuild-cubes.R`, `cube-delta.mjs`, `cubes-manifest.mjs` (1.1.0 lá: bloco `tables` assinado), `publish-cubes.sh`, os 3 geradores das tabelas (`cid9-tables.py`, `csap-universe-tables.py`, `estudo-1992-1997-cnv.py`) e os 34 sidecars de `data/` — o ESTADO dos cubos é o canal (`sih/cubos/manifest.json` + sidecars), não o git; o produtor baixa o sidecar publicado antes de cada rebuild. Tudo vive em `healthbr-data/scripts/pipeline/sih-cubos/` (receita no README de lá; `docs/reference-pipelines-pt.md` §16) e roda por `rebuild-sih-cubes.yml` (cron terça 06:00 UTC + dispatch do `sync-check.yml` após cada manutenção, em vez do `repository_dispatch` cruzado com PAT). Os gates foram junto e um deles é ESTE repositório: o job `decide` mede o frescor com o `scripts/freshness-check.mjs` daqui sobre os sidecars do canal, e o job `build` faz checkout do sih-br-mcp (público desde 08/09) e roda o `smoke-stdio.mjs` sobre os cubos novos antes de publicar. Tabelas de classificação viraram contrato versionado no produtor, publicado em `sih/cubos/tables/*.json` com SHA-256 no manifesto; `src/data/` guarda CÓPIAS (o TS importa em build) e `scripts/tables-check.mjs` confere no CI (offline contra `tests/fixtures/sih/cubes-manifest-excerpt.json`, ao vivo contra o canal com aviso se o portal cair). Os insumos gitignored dos geradores (`TAB_SIH_199201-199712.zip` do FTP do DATASUS) ganharam cópia no canal em `sih/cubos/insumos/` — a cadeia de reprodução fecha sem o FTP. Provas: 2023/RR construído localmente pelo builder migrado = fixture daqui byte a byte (3 cubos); 2025 reconstruído no runner do healthbr-data com delta zero e manifesto regenerado. Ficou: `scripts/build-population.R` (população ainda não está no canal — item próprio), `freshness-check.mjs` (consumidor e produtor usam), `smoke`/`golden`/fixture; `package.json` segue `private: true` até o item de publicar no npm. Fecha `sih:produtor-healthbr-data` no portfolio-monitor.
28. **População no canal — servidor 0.12.0 baixa os denominadores como baixa os cubos** (2026-09-08, 38ª sessão; ordem única do usuário dada no fim da 37ª: `sih:populacao-no-canal`). Até aqui a população ficava só em `data/` local (`DATA_DIR` cru, nunca o cache): o servidor instalado pelo npm respondia "Execute build_population.R primeiro" em `get_hospitalization_rates`, em `compare_icsap_trends` com `rate_per_10k` e devolvia `population_years: null` em `get_available_years`; e `populationProvenance()` mentia (`retrieved_at` era a constante 2026-01-23 e a fonte declarada era a SIDRA 7358 rev. 2018, quando o build já era a Projeção 2024). **Produtor (healthbr-data, PR `sih-populacao`):** `scripts/build-population.R` migrou para `scripts/pipeline/sih-cubos/` como CLI (`--out`/`SIH_CUBOS_OUT`; csapAIH do GitHub — não está no CRAN; 3 tentativas por ano no FTP do DATASUS e ABORTA se faltar um dos 34 anos — o `tryCatch` original só avisava; contagens conferidas; `POP_UF_ULTIMO_ANO = 2025` continua constante explícita, decisão humana; sidecar `pop_provenance.json` com built_at, last_year, fontes, linhas/anos/UFs/Brasil por arquivo, pacotes; legado SIDRA 2018 removido — fica no histórico daqui). `cubes-manifest.mjs` 1.2.0 assina `pop_uf`, `pop_uf_agregado`, `pop_municipios` e o sidecar no bloco `population` (herdado do manifesto anterior sem a flag, como `tables`; `--verify` confere cada arquivo com DuckDB contra o sidecar; `--years none`); `publish-cubes.sh` ganhou a 4ª posição. Workflow novo `build-sih-population.yml` (só `workflow_dispatch`; ~4 min; mesmo concurrency group dos cubos): gates = 34 anos + planilha (no script), regra do CONTEXT (`last_year` ≤ maior ano com `window_complete` no canal) + contagens, smoke DESTE servidor com a população nova, `--verify` no publish. Prova: build local = parquets de `data/` linha a linha nos três arquivos (`EXCEPT ALL` = 0; `pop_uf` byte a byte). **Consumidor (0.12.0):** `cache.ts` ganha `ensurePopulation()` (quatro arquivos por `downloadVerified`, idempotente, sem corrida; `available=false` quando o manifesto não tem o bloco — não é erro do canal), `populationPresent()`, `cachedCubesManifest()` (síncrono, para a proveniência); `duckdb.ts` ganha `getPopulationDir()` — a pasta configurada tem precedência sempre que tiver `pop_uf.parquet` (fixture, build à mão), senão o cache — usada por `getPopulationPattern`/`hasPopulationData`/`hasPopUf`/`yearRangeOf`, `resetPopulationCoverageCache()` (a cobertura memoizada `null` antes do download) e a mensagem única `POPULATION_MISSING_MESSAGE` (aponta o canal, não um script R); `index.ts` chama `ensurePopulationForTool()` ANTES do switch e fora do `if` dos cubos (`get_available_years` está em `TOOLS_WITHOUT_CUBES` e relata `population_years`) para `get_hospitalization_rates`, `compare_icsap_trends`, `get_available_years`; falha de rede não derruba a chamada (stderr + "sem denominador"). `populationProvenance()` lê `population.built_at`/`builder_version` do manifesto carregado e declara IBGE Projeção 2024 + DATASUS POPBR/POPSVS; sem manifesto (cache desligado: fixture, golden) vale a safra da fixture (`POPULATION_BUILT_AT` = 2026-09-07, data real do `pop_uf` da fixture). **Golden regravado no mesmo commit**: só os 5 blocos de proveniência da população (source, source_url, license, retrieved_at, data_vintage, citation), nenhum valor. `cache-selftest` ganhou 9 casos (baixa 4, idempotente, adulterado recusado sem `.part`, manifesto sem bloco → `available=false`). **Defeito de classe achado de passagem:** `smoke-stdio.mjs` comparava o baseline byte a byte e reprovava em checkout Windows (`i/lf w/crlf`) sem a superfície ter mudado — normaliza CRLF agora (mesma classe do tables-check, [[autocrlf]]). `scripts/build-population.R` SAI daqui. README, este arquivo (regras de população, esquemas, estrutura, ambiente).
29. **Servidor remoto por Streamable HTTP para o claude.ai — SDK v2, factory e dois transportes sobre o mesmo despacho** (2026-09-09, 39ª sessão; ordem única do usuário dada no fim da 38ª: `sih:servidor-remoto`; PLAN-004; host decidido pelo usuário em 08/09: Cloudflare Containers). O sih era só stdio (SDK v1, `Server` de baixo nível instanciado uma vez no topo de `index.ts`, `switch` por nome, zero anotações, `SERVER_VERSION` fixo em "0.10.0" desde a 0.10.0 — o handshake e a citação da proveniência anunciavam versão errada nas 0.11 e 0.12). **Código (0.13.0):** `src/index.ts` virou `src/tools.ts` (as 12 ferramentas + `callTool()`, o ÚNICO caminho de execução: população, cache dos cubos, switch, proveniência); `src/server.ts` é a factory `createServer()` no SDK v2 (`@modelcontextprotocol/server` 2.0.0: `McpServer.registerTool` com `title`, anotações `readOnlyHint/idempotentHint` e `fromJsonSchema` sobre os JSON Schemas que já existiam — o baseline de superfície NÃO mudou); `src/index.ts` é a entrada stdio (`serveStdio`, bin do npm); `src/http.ts` é a entrada Streamable HTTP (`NodeStreamableHTTPServerTransport`, STATELESS: servidor e transporte novos por request; `/mcp` e `/healthz`; resposta em SSE porque a série de 34 anos leva 30 s). `SERVER_VERSION` lê o package.json. SDK v1 removido; scripts no cliente v2. **Prova:** `smoke:stdio` passou contra o MESMO baseline; `smoke:http` (novo, no CI) lista e chama as mesmas três ferramentas por HTTP contra o mesmo baseline; golden regravado por dois motivos, nenhum numérico: a citação passa a dizer `v0.13.0` (defeito da versão fixa) e `rank_csap_groups {limit: 50}` passa a ser RECUSADO com `-32602` "limit must be <= 19" — o schema sempre disse máximo 19 e o 0.12.1 não validava entrada; a validação do SDK v2 é o contrato publicado valendo, mantida de propósito. **Container:** `Dockerfile` (node:22-slim, duas etapas, população pré-baixada na imagem por `scripts/warm-cache.mjs` porque o disco do Containers é EFÊMERO; cubos sob demanda) e `worker/` (Worker de borda do template do commons: rotas públicas, bearer opcional, rate limit, Analytics Engine `sih_mcp_tool_calls`, `/mcp` encaminhado ao container). Deploy, domínio `sih.sidneybissoli.com`, Workers Paid e o conector no claude.ai são gates do usuário (PLAN-004 F2–F4). Fora de escopo: `outputSchema` por ferramenta, OAuth, Tasks.
30. **Deploy do servidor remoto (0.13.1): consulta ICSAP ano a ano, porque o container de 1 GiB não cabia a série** (2026-09-09, 41ª sessão; ordem única do usuário: F2 deploy + F3 + F4). **Deploy:** `cd worker && npx wrangler deploy` (Docker Desktop 4.90, wrangler 4.130) publicou `sih-br-mcp-edge` + container `sih-br-mcp-edge-sihcontainer` (`basic`, imagem 571 MB) em 72 s; `https://sih.sidneybissoli.com/mcp` lista as 12 ferramentas (2,9 s a quente); cold start medido 16,6 s (handshake com o container dormindo). O primeiro `npm ci` dentro do build da imagem morreu com ECONNRESET aos 434 s — transitório (repetido: 6,6 s). **Defeito achado na prova real:** `compare_icsap_trends` 1992–2025 por UF falhava em 370 s, e até 2015–2025 em 210 s, com `Out of Memory Error: failed to offload data block (1.5 GiB/1.5 GiB used)`: o SQL de `icsapAggregateSql` lê a CTE `linhas` duas vezes (DISTINCT dos estratos + soma das ICSAP), o DuckDB a materializa com todas as colunas e derrama para o disco temporário; no `basic` a memória do DuckDB é 819 MiB (cgroup) e o disco livre 1,5 GiB (4 GB − imagem − 1,6 GB de cubos). Nos 32 GB locais isso nunca apareceu (34 anos em 8 s). Reproduzido em Docker local com `--memory=1g` e os cubos DO CANAL (os de `data/` estavam defasados: 2025 tem 11,6 MB local × 16,4 MB no canal, builder 2.7.0): mesma falha. Variantes medidas sob 1 GiB: uma leitura só sem CTE também falha; ano a ano com 1 thread passa (34 anos por UF em 62 s a 0,5 CPU), mesmos números que a execução única sob 4 GiB. **Correção (0.13.1):** `queryIcsapAggregate` em `src/db/duckdb.ts` — com mais de um ano, cada ano entra numa tabela temporária (agrupamento pedido + `year`) e a agregação final (somas, `icsap_percentage`, `buildOrderBy`, `LIMIT`) roda no DuckDB sobre ela; um ano só segue o caminho direto; `queryIcsap` e `calculateIcsapIndicators` passam por ela. Equivalência provada em 7 cenários (year×uf, uf com limit, csap_group com universe all, totais sem grupo na fronteira 1996–1999, cid_revision×year, filtros uf+csapGroups por sex, um ano) contra o SQL antigo, byte a byte; golden regravado só pela citação `v0.13.1`; smoke stdio/http contra o mesmo baseline. `SIH_DUCKDB_THREADS` (padrão 4; a imagem fixa 1). **Segundo defeito:** no rollout o SIGTERM chegou no meio da série e `shutdown()` esperava `closeDatabase()` (closeSync do DuckDB espera a consulta): o processo velho drenou ~10 min e toda chamada respondia "The container is not running, consider calling start()" — `shutdown()` agora sai em 5 s (`setTimeout(process.exit).unref()`). **Medido depois:** série 1992–2025 por UF pela borda com os 34 cubos no disco = 310 s (1/4 vCPU: 5× o bench local a 0,5 CPU), 2024–2025 por UF = 25,5 s; a frio (download de 1,5 GB + consulta) a conexão caiu em ~322 s sem resposta (limite ainda não identificado: o Node tem `requestTimeout` 300 s + verificação a cada 30 s, e a plataforma pode cortar a ligação Worker→container ociosa; a primeira execução, na 0.13.0, chegou a devolver em 370 s). Conclusão: a memória está resolvida no `basic`; o que falta para a série longa no claude.ai é CPU ou pré-agregação — `sih:serie-pre-agregada` (estratos distintos por ano gravados no produtor ou no consumidor ao baixar o ano), anotado no TAREFAS. **F3:** `host = "sih.sidneybissoli.com"` em `config.R:47` do monitor. **F4:** ver TAREFAS.
31. **Série pré-agregada da ICSAP — resumo e estratos do canal (PLAN-005, 0.14.0)** (2026-09-09, 41ª sessão; usuário aceitou a opção (b) do item `sih:serie-pre-agregada`). O custo dominante da agregação ICSAP era o DISTINCT dos estratos refeito a cada consulta (26,5 s dos 28 s locais; 310 s na borda). **Produtor** (healthbr-data PR #5, `derive-icsap-summary.mjs` + `build-sih-summary.yml`): deriva dos cubos PUBLICADOS (função pura, sha256 conferido no download) o `sih_icsap_resumo.parquet` (universe × year × uf × cid_revision × csap_group + n_total do denominador; TODOS os anos, 276 KB, 35.887 linhas) e `sih_icsap_estratos_YYYY.parquet` (o DISTINCT gravado, ~2,5 MB/ano), assinados no bloco `icsap_summary` do manifesto **1.3.0** com FRESCOR por sha256 (`derived_from` por ano tem de bater com o cubo publicado; manifesto recusa resumo velho; herdado e velho gera aviso). Regra operacional: rodar `build-sih-summary.yml` após todo rebuild de cubos. **Consumidor (0.14.0):** `ensureIcsapSummary`/`ensureEstratosYears` em cache.ts (nunca lançam; pré-agregado é otimização); `queryIcsapAggregate` roteia — grão do resumo e anos frescos → `resumoAggregateSql`; senão ano a ano com denominador dos estratos quando presentes; `lastIcsapAggregatePath()` expõe a rota; chamada coberta pelo resumo NEM BAIXA cubos, e `compare_icsap_trends` valida anos pela união cubos locais + anos frescos. **Prova:** `scripts/summary-equivalence.mjs` (no CI): igualdade byte a byte com o caminho clássico em 10 cenários, na fixture e nos 34 cubos do canal; golden só mudou na citação. **Medido na borda (deploy 0a561234):** série 1992–2025 por UF A FRIO (container dormindo, disco vazio) = 6,4 s total (4,8 s cold start + 1,1 s a chamada — baixa manifesto + 276 KB, zero cubos); a quente 1,1 s; RR/2023 0,9 s; consulta fina de 2 anos a frio 31 s (baixa só os 2 cubos). Era 310 s a quente e falha a frio na 0.13.1. Células conferidas contra a 0.13.1 (1992 AC/AL/RO/RR/SP, 2025 TO): idênticas. Armadilha: o `data/` local está DEFASADO do canal (sem cid_revision em anos antigos) — a prova de 34 anos roda sobre os cubos baixados do canal.
32. **Cache ciente do tipo de cubo + trends anual pelas séries (0.14.1)** (2026-09-09/10, 41ª sessão; achado pelo teste do usuário no chat: "total do Brasil desde 1992" levou 4 min). Três defeitos somados no caminho não-ICSAP: (1) `ensureYear`/`ensureYears` baixavam os TRÊS cubos de cada ano (~1,6 GB) para consultas que leem um tipo — agora recebem `kinds` e o `callTool` passa `TOOL_CUBES` (trends → series; hospitalizations/compare_regions/rates → causas; as 4 ICSAP → icsap); `yearsPresent` idem; `getAvailableYears` = união dos três tipos. (2) `yearsFromArgs` não conhecia o par `year_start`/`year_end` do trends (só `start_year`/`end_year` do compare) → trends caía no "sem ano" e baixava a série inteira do canal. (3) o trends ANUAL lia o cubo de CAUSAS (~55 MB/ano) quando o de SÉRIES (~40 KB/ano) tem as mesmas medidas — `querySeriesYearly`, igualdade provada grupo a grupo (year × uf × cid_chapter, EXCEPT = 0 nos 34 anos e na fixture; o manifesto já verifica SUM(n) causas = séries por ano), golden byte a byte. `ensureYears` baixa anos em LOTES de 4 (a latência por requisição dominava: 68 arquivos ≈ 68 s em série); arquivos de UM ano seguem sequenciais (contrato do selftest: ano recusado não deixa rastro). cache-selftest com cenário por tipo. **Medido:** local com cache vazio, trends 2019–2025 caiu de 67,5 s (68 arquivos) para 5,1 s (14); na borda, A MESMA pergunta do chat a frio (disco vazio) = 6,0 s (era 4 min) e a quente 1,8 s; série ICSAP de 34 anos segue 1,8 s. Armadilha nova: o `wrangler deploy` falhou uma vez com "Docker build exited with code 1" sem detalhe no log — o `docker build` direto passou e o deploy seguinte também (transitório, como o ECONNRESET da decisão 30).
33. **Notas de era que sumiam, teto de resposta e cubo leve (0.14.2)** (2026-09-10, 41ª sessão; avaliação de fragilidade pedida pelo usuário). Três achados. **(1) DEFEITO DE CORREÇÃO — as ressalvas sumiam.** As notas que tornam 1992–1997 interpretável (lista CID-9 DERIVADA e não oficial, com g03/g05 não comparáveis; `uf` do ARQUIVO; `value` nominal pré-real; datas imputadas) saem do SIDECAR, e sumiam por duas causas somadas: `loadSidecars()` memoizava resultado VAZIO (array vazio é truthy em JS), fossilizando o vazio para o resto do processo em todo container que leu a pasta antes do primeiro download; e o caminho do resumo (PLAN-005) não baixa cubo, logo não trazia sidecar. Medido: a série de 34 anos na 0.14.0, a frio, devolveu só a nota de universo. O risco estava escrito no PLAN-004 §6 e nunca fora verificado. Conserto: memoizar só resultado NÃO vazio; `ensureSidecars()` (0,3 MB/ano) no caminho do resumo; `resetSidecars()` após todo download. Provado em produção: 1992–1997 pelo resumo devolve as 4 notas. **(2) TETO DE RESPOSTA.** `get_icsap` por município × grupo CSAP em UM ano devolvia 83.769 linhas e 17,9 MB. Teto de 5.000 linhas no FUNIL (`capData` em `callTool`: vale para as 12 e para as futuras), cortando pela ordenação determinística que a consulta já tem, com aviso de quantas ficaram de fora e de que os totais do `summary` são do conjunto inteiro. A maior resposta do golden tem 5,3 KB, então o teto não encosta em nada existente. Na borda: 2,03 MB. **(3) CUBO LEVE onde cabe.** `compare_regions` e `get_hospitalization_rates` devolvem só contagem e óbitos, que o cubo de séries tem (1,2 MB nos 34 anos contra 1.253 MB do de causas); passam a lê-lo quando a chamada não pede sexo, idade, raça, município nem o filtro de sensíveis. `get_hospitalizations` fica no pesado porque devolve dias e valor. `scripts/series-routing-equivalence.mjs` (no CI) exige resposta idêntica em 8 recortes e confere 9 decisões de rota; golden byte a byte. Taxas de 7 anos pela borda: 1,2 s. **Armadilha de MEDIÇÃO (errei 3 vezes na mesma noite):** a sonda imprime a linha de log com os ARGUMENTOS antes da resposta, e os argumentos são JSON válido com as MESMAS chaves (`indicator`, `year`) — o parser ancorado na PRIMEIRA ocorrência lia os argumentos e concluía “zero notas”, acusando defeito onde não havia. Ancorar depois da linha de log e nunca truncar o payload que se vai medir.
34. **Cubo de causas pré-agregado — grão A e grão B do canal (PLAN-006, 0.15.0)** (2026-09-10, 42ª sessão; ordem única do usuário). O cubo de causas era o último caminho caro: **1.253 MB** nos 34 anos, contra 389 MB da ICSAP (com resumo desde o PLAN-005) e 1,2 MB das séries — e é ele que responde as perguntas mais naturais (quantas internações, quanto o SUS gastou, quais as principais causas, taxa por 100 mil). O canal passa a publicar dois atalhos derivados dele, no bloco `causas_summary` do manifesto **1.4.0**: **grão A** (`sih_causas_resumo.parquet`, um arquivo com todos os anos, 569 KB / 47.790 linhas) no grão `year × uf × cid_chapter × cid_revision × is_csap × exclusion` com `n`, `days`, `value` e `deaths`; e **grão B** (`sih_causas_estratos_YYYY.parquet`, 18,8 MB nos 34 anos) que acrescenta `sex`, `age_group` e `race`. **Os dois grãos, não um:** o A responde sozinho a série longa, e publicar só o B faria toda pergunta simples pagar 19 MB. **A faixa etária é a MESMA de `pop_uf_agregado.parquet`** (`0-4` … `75-79`, `80 e +`), então `aggregatedAgeGroupsFor()` — que já decidia o recorte da taxa antes de 2000 — decide também esta rota: recorte que não alinha nas faixas não é recusado, cai no cubo e sai exato. **Fora do grão de propósito:** mês, categoria CID de 3 dígitos e grupo CSAP (o grão com a categoria de 3 dígitos custaria 333 MB e 694 s de derivação — medido e rejeitado). Roteamento conservador em `causasRoute()` (`src/db/duckdb.ts`) e o espelho no nível dos ARGUMENTOS em `causasSummaryCoversCall()` (`src/tools.ts`), que é onde se decide baixar 1,25 GB ou 569 KB; `seriesFitsCall` tem precedência, porque o cubo leve é mais barato ainda. Medido com uma thread, como no container: "internações, dias, gasto e óbitos por ano, 34 anos" custa **0,01 s pelo grão A contra 5,50 s pelos cubos**, com resposta idêntica. Prova: `scripts/causas-routing-equivalence.mjs` (no CI) exige resposta byte a byte igual à do cubo em 24 cenários e confere 21 decisões de rota — verde na fixture e nos 34 anos do canal; golden inalterado. **Risco do §6 do plano, tratado aqui:** o grão A torna trivial somar 1992 com 2025 sem perceber que um está em cruzeiro — a nota de era passa a dizer explicitamente quando a consulta CRUZA a fronteira da moeda e que o `total_value` do `summary` não tem significado econômico (medido: 1992+2025 devolve R$ 18,5 trilhões). No produtor, o derivador confere CADA ANO do grão B contra o cubo nas quatro medidas e o grão A contra os mesmos totais, e o manifesto recusa resumo derivado de cubo que não é o publicado. **NA BORDA (0.15.0, canal 1.4.0):** a série de 34 anos com gasto passou de **54,7 s para 0,6 s**, os principais capítulos na série longa de **58,7 s para 0,2 s**, e o recorte demográfico de 3 anos de 8,4 s para 1,4 s — com CADA NÚMERO idêntico ao da 0.14.2. Custo frio medido sem depender da instância: 9,7 s e 10,3 MB (o grão A mais os 34 sidecars, nenhum cubo), abaixo do alvo de 10 s. **Armadilha:** medir logo depois de um `wrangler deploy` mede o ROLLOUT — a primeira sonda travou 900 s e depois deu "container is not running"; a mesma pergunta respondeu em 0,6 s minutos depois. E no cliente MCP 2.0.0 a assinatura é `callTool(params, options)`: opções na terceira posição são ignoradas em silêncio e o teto de 60 s continua valendo. **SIDECARS PRÉ-ASSADOS (mesma sessão):** os 34 sidecars eram 9,7 dos 10,3 MB do custo frio — pequenos, muitos, sempre pedidos e baixados em série. Passaram a ir na imagem do container junto com os dois resumos, ao lado da população que já ia; medido na imagem de verdade sob os limites do `basic`, a série de 34 anos a frio caiu de **13,4 s (10,3 MB) para 2,5 s (sem baixar nada)**, com a imagem indo de 571 MB para 584 MB. Assar exigiu `ensureSidecars()` conferir por **SHA-256** e não só por tamanho (memoizado por processo): pré-assado, um sidecar reescrito por um rebuild que mantivesse o mesmo número de bytes serviria a nota de era do build anterior — número certo, ressalva errada. O `cache:selftest` ganhou esse caso.

---

35. **O cubo só lê cubo: o glob que somava os pré-agregados junto (0.15.2)** (2026-09-10, 42ª sessão; achado por um USUÁRIO na primeira pergunta real feita ao conector, não pelas provas). `getParquetPattern` devolvia o glob `sih_<tipo>_*.parquet`, e os PRÉ-AGREGADOS que o servidor baixa para a MESMA pasta (`sih_causas_resumo.parquet`, `sih_causas_estratos_YYYY.parquet` e os equivalentes da ICSAP) casam com esse padrão. Com `union_by_name` eles entravam na leitura do cubo e somavam junto, em silêncio: `get_hospitalizations` agrupado por ano e mês devolveu **26.648.728 internações em 2023, o DOBRO das 13.324.364 reais**, numa linha de mês NULO que não deveria existir (o pré-agregado não tem a coluna `month`). Anos com resumo E estratos em disco dobravam; os demais ganhavam uma linha fantasma com o total do ano. Na ICSAP o mesmo choque existe desde a 0.14.0, latente: reproduzido, RR/2023 cai de 24,7 % para **7,56 %** — em produção estava mascarado porque o denominador vem do arquivo de estratos por caminho explícito e as medidas têm nomes diferentes (`n` contra `n_icsap`), dois acidentes que se cancelam e deixariam de cancelar assim que um ano tivesse o resumo sem os estratos. **Causa raiz:** o leitor do cubo era definido por PADRÃO DE NOME num diretório que passou a ter vizinhos com nomes parecidos. **Conserto no funil:** `cubeFiles()` lista os arquivos de ANO por nome exato (`^sih_<tipo>_\d{4}\.parquet$`) e `cubeSource()` devolve a LISTA para o `read_parquet`, nunca um glob; a rota de fallback do `causasRoute` virou preguiçosa, senão uma chamada coberta pelo grão A estouraria por não haver cubo em disco. **Por que nenhuma prova pegou:** `equiv:summary`, `equiv:series` e `equiv:causas` derivam os pré-agregados numa pasta TEMPORÁRIA — os dois lados nunca conviviam. `scripts/cube-isolation.mjs` (`npm run cube:isolation`, no CI) põe os dois na mesma pasta e exige resposta idêntica em 9 consultas, mais a asserção de que ler pelo glob AINDA daria número diferente (sem ela o teste passaria numa pasta vazia, provando nada). Contra a implementação velha ele reprova em 8 pontos, inclusive no cubo de SÉRIES. **Lição:** ao publicar artefatos novos numa pasta que já é lida por padrão de nome, o padrão vira acoplamento — e nenhum teste que monta os artefatos em outro lugar consegue ver isso.
36. **As superfícies públicas passam a carregar o vocabulário da pergunta (0.15.4)** (2026-09-13, 48ª sessão; item `geo:sih-br-mcp` do monitor, ação aprovada pelo usuário em 24/08). O monitor GEO mediu três rodadas (24/08, 31/08, 07/09) em quatro motores × duas variantes: **zero citações** deste servidor em todas as 24 células, e no lugar dele `microdatasus`, `PySUS`, `read.dbc`, `csapAIH` e `brpop`. A hipótese registrada era de vocabulário, e a inspeção confirmou: o título era `# sih-br-mcp`, a descrição do repositório no GitHub estava **vazia**, e "DATASUS" só aparecia dentro do diagrama de proveniência e na licença — nada no topo dizia "internações hospitalares", "AIH" ou "DATASUS", que são as palavras das perguntas reais (as 7 sondas do monitor usam SIH, AIH, DATASUS, internações, CID-10, taxas por município, TabNet). Também não havia uma linha em inglês, e duas das sondas são em inglês. **O que mudou:** README com H1 descritivo, abertura com o vocabulário da fonte, um bloco `In English`, a seção "Perguntas que ele responde" (a pergunta como o usuário a faz, com a ferramenta que a atende) e a comparação honesta com as alternativas; `docs/comparativo-alternativas.md` com a cadeia do dado, três perguntas lado a lado e os volumes medidos na fonte (microdado bruto 23,67 GiB em 11.157 partições; cubos 1,17 GiB; pergunta de 34 anos 10,3 MB e 0,6 s); `description` e `keywords` do npm; `title` e `description` do `server.json` (92 de 100 caracteres, o limite que o registro impõe); `glama.json` declarando o mantenedor, como nos seis irmãos. **O que NÃO mudou, de propósito:** o nome do pacote. A ação aprovada dizia "renomear/descrever"; renomear `sih-br-mcp` quebraria as instalações e o id `io.github.SidneyBissoli/sih-br-mcp` do registro, e o ganho de vocabulário está no título e nas descrições, que são o que os diretórios e os motores citam. **Prova:** o golden das 12 ferramentas mudou só na citação (`v0.15.3` → `v0.15.4`, as mesmas 30 linhas idênticas), nenhum número; o CI é quem confirma, porque `SERVER_VERSION` lê o package.json em tempo de execução.

37. **Suíte de testes (vitest): decisão de rota e envelope de saída — e por que NÃO há `outputSchema`** (2026-09-14, 50ª sessão; ordem do usuário: `campanha:contrato-vitest`, "montar vitest no sih-br-mcp e aplicar o gate de contrato de saída, no molde de bcb-br-mcp e ilo-mcp-server"; a escolha entre (A) e (B) abaixo foi dele, no início da sessão). **O achado que mudou a forma da tarefa:** nenhuma das 12 ferramentas declara `outputSchema` (`grep -rn outputSchema src/` = zero). O gate irmão do ilo e do bcb afirma exatamente *"o `structuredContent` obedece ao `outputSchema` anunciado"* — sem esquema anunciado não há o que validar, e pela spec do MCP a obrigação só nasce quando o `outputSchema` existe. O servidor não viola nada hoje; está desprotegido de outra forma. **Decisão do usuário: (A) agora, (B) como item próprio** — suíte sobre as funções puras e sobre o ENVELOPE, sem tocar no contrato publicado, mais a MEDIÇÃO que instrui (B). **O que entrou:** `vitest` ^5 + `vite` ^8 (peer da v5) em devDependencies, `vitest.config.ts` (fixtures por `SIH_DATA_DIR`, `SIH_CUBES_CACHE=off` para a rede nunca ser tocada, `fileParallelism: false` porque a conexão do DuckDB é de processo), `npm test` e um passo no `ci.yml` antes dos gates de ponta a ponta (é o mais barato, ~1,5 s, e falha apontando a linha em vez de um diff de baseline de 127 KB). **`tests/routing.test.ts` (42 casos):** interroga a DECISÃO pura — `causasRoute` (PLAN-006), `resumoEligible` (PLAN-005), `buildWhereClause`, `buildOrderBy`, `groupSelect`/`groupKey`, `universeCondition`, `aggregatedAgeGroupsFor`. É onde este repositório já se machucou duas vezes (cubo errado escolhido; glob lendo o artefato vizinho) e o que nenhum gate anterior alcançava: equivalência só flagra quando os dois caminhos existem E discordam; aqui a regra é afirmada sozinha, sem disco e sem DuckDB (o estado dos pré-agregados é injetado por `vi.mock` sobre `causasSummaryState`/`icsapSummaryState`). Sete auxiliares puros de `src/db/duckdb.ts` passaram a ser exportados só para isso — nada disso entra no contrato MCP. Casos que valem citar: idade ZERO e `is_csap: false` viram condição (a guarda é `!== undefined`, não truthiness); o resumo que existe mas NÃO é fresco para o ano cai no cubo; falta de UM estrato derruba o lote inteiro para o cubo. **`tests/output-contract.test.ts` (27 casos):** o ENVELOPE das 12, com um caso CHEIO e um caso MAGRO cada, pelo servidor de verdade (`createServer`) em transporte EM MEMÓRIA. O golden pina VALORES do caminho feliz com fixture cheia; o defeito mora na resposta com os campos opcionais AUSENTES, que é onde `JSON.stringify` apaga a chave e o cliente lê "missing required property" — por isso o conferidor serializa o `structuredContent` por conta própria antes de comparar com o texto. Afirma: `structuredContent` presente (o funil de `withProvenance` não foi pulado por caminho rápido — já aconteceu aqui, quando o atalho cortou o sidecar dos avisos), proveniência completa nos cinco campos, `attribution` não vazia, e `content[0].text` exatamente igual ao JSON da estrutura depois do fio. O conferidor é escrito de fora, sem importar nada de `src/provenance.ts`: guarda que reusa o padrão do que vigia concorda com o defeito. Tem o teste que prova que o portão FECHA (cinco mutilações de uma resposta real, cada uma recusada) e o que exige caso cheio e caso magro para toda ferramenta que o `tools/list` publicar — derivado da superfície, não de lista à mão, para a suíte não envelhecer calada. Herdou também o contrato de ENTRADA: as 12 recusam chave desconhecida e a recusa NOMEIA a chave. Prova de que a suíte reprova: duas mutações no código vigiado (tie-break do `buildOrderBy` removido; guarda de `months` do `causasRoute` removida) derrubaram 4 casos e o verde voltou com o código restaurado. **A MEDIÇÃO para (B)** (`scripts` de sessão, resultado aqui): 54 chamadas cobrindo variações de `group_by`, filtro, universo e ano ausente produzem **16 formas distintas em 12 ferramentas — 9 delas com UMA forma só**, inclusive `get_hospitalizations` (8 variações de `group_by`, uma forma). As três que variam: `list_csap_groups` (2 — `{group}` quando se pede um grupo, `{groups,source,total_groups}` na lista), `get_hospitalization_rates` (3) e `compare_icsap_trends` (2), e nestas duas a variação é inteiramente o caminho de "ano sem dado" (`error` + `available_sih_years`). **Conclusão que corrige a estimativa do dossiê:** (B) é bem mais barato do que se supunha — a forma NÃO varia com `group_by`; varia com o caminho de erro-mole. Declarar `outputSchema` é, para 9 ferramentas, transcrever uma forma fixa. **Achado colateral, NÃO corrigido (item próprio, decisão do usuário pendente):** para um ano que não existe (2030, ou qualquer ano fora do canal), 6 das 8 ferramentas de dado respondem ZERO CALADO — `get_hospitalizations({year:[2030]})` devolve uma linha de medidas nulas e `summary.total_hospitalizations: 0`, sem `error` e sem nota, o que se lê como "não houve internação" e não como "não tenho esse ano". Só `get_hospitalization_rates` e `compare_icsap_trends` avisam, com `error` + `available_sih_years`. Causa-raiz: a guarda existe DENTRO dessas duas (nasceu da validação do denominador populacional) e nunca foi generalizada; o funil `callTool` (src/tools.ts) só erra quando NÃO HÁ nenhum cubo local. Camada certa do conserto: o funil, depois de resolver os anos pedidos. Não foi feito nesta sessão porque muda o que 6 ferramentas RESPONDEM e viaja no próximo bump — é decisão de contrato, como (B).

38. **Ano que não existe deixa de ser respondido com zero — a guarda sobe para o funil** (2026-09-14, 50ª sessão; autorização do usuário depois de a mesma classe ter sido fechada no senado-br-mcp). **O defeito**, achado ao varrer a classe do "zero calado" nos sete servidores do portfólio: `get_hospitalizations({year:[2030]})` devolvia uma linha de medidas NULAS e `summary.total_hospitalizations: 0`, **sem `error` e sem nota** — o que se lê como "não houve internação em 2030", e não como "não tenho 2030". **Seis das oito** ferramentas de dado se comportavam assim; só `get_hospitalization_rates` e `compare_icsap_trends` avisavam. **Causa-raiz:** a guarda existia DENTRO dessas duas, nascida da validação de cobertura do denominador populacional, e nunca subiu para o funil; `callTool` só errava quando não havia NENHUM cubo local (`unavailable.length && downloaded.length === 0 && getAvailableYears().length === 0`), condição que praticamente nunca se satisfaz num servidor em uso. Zero é a resposta errada mais perigosa que existe aqui: atravessa o esquema, atravessa o golden (que pina o caminho feliz com fixture cheia), atravessa o cliente e chega ao leitor com cara de número medido — um erro se conserta, um zero calado se cita. **O conserto, no funil e não tool a tool**, porque foi exatamente a guarda que morava só em duas delas que deixou as outras seis desprotegidas: em `callTool`, depois do bloco de cache, os anos pedidos (`yearsFromArgs`, que já cobre `year`, `years` e os dois pares de intervalo) são confrontados com os anos ATENDÍVEIS; nenhum atendível devolve `{error, data: [], available_sih_years, note}` — **a mesma redação que as duas honestas já usavam**, reusada em vez de inventar um terceiro vocabulário, que é o que faz as doze soarem como um servidor só. **A sutileza que decide a correção, e que quase a fez recusar consulta válida:** "atendível" NÃO é `getAvailableYears()`, que só enxerga os cubos (`sih_(causas|series|icsap)_YYYY.parquet`). Uma chamada coberta pelo grão A ou B do PLAN-006 responde SEM cubo nenhum em disco — é o motivo de o caminho rápido existir —, e guardar só pelos cubos recusaria justamente as consultas que o pré-agregado veio acelerar. A conta é a UNIÃO dos cubos locais com os `freshYears` dos dois resumos (causas e ICSAP), e a guarda roda DEPOIS do bloco de cache, que é onde os anos pedidos são baixados ou cobertos. Conservadora por desenho: na dúvida responde, e só acusa o ano que não está em lugar nenhum. **O caso PARCIAL, que nem as duas honestas tratavam:** elas filtravam para `validYears` e seguiam caladas — uma série pedida de "2022 a 2023" voltava cobrindo só 2023, com o mesmo aspecto de uma série completa. Isso ESTREITA A PERGUNTA sem avisar, e agora o funil acrescenta `years_not_available: {years, note}` dizendo as duas metades: o que ficou de fora e o que os números cobrem. **Medição antes e depois, pela mesma varredura:** 2 de 8 avisavam → **8 de 8**. **Gate:** `tests/ano-ausente.test.ts` (5 casos), com a lista de ferramentas derivada do `tools/list` — ferramenta nova que aceite ano entra sozinha e reprova até se defender — e com os contrapesos que impedem o teste de virar decoração: metadados não afetados, caminho normal sem chave a mais, e o **zero LEGÍTIMO** (ano que existe, recorte sem nenhuma linha) continuando a responder com números, porque "não tenho o ano" e "tenho o ano e o recorte não casou nada" são coisas diferentes. Provado por mutação: desligar a guarda derruba os dois casos centrais. **Baseline do golden regravado** (3 casos, estável em duas execuções): os dois de 2022–2023 ganham `years_not_available`, e o de `get_hospitalization_rates {year:[1995], age_min:0, age_max:17}` troca a mensagem de faixa etária pela de ano ausente — o funil pega o bloqueio mais fundamental primeiro. Nota: esse caso só muda NA FIXTURE, que tem apenas 2023; em produção 1995 existe (a série vai a 1992), o cubo é baixado e a mensagem de faixa volta a ser a que aparece. Os 11 gates anteriores seguem verdes, inclusive os três de equivalência, que são os que exercitam o caminho pré-agregado.

39. **`outputSchema` nas doze ferramentas e o gate de contrato de saída ao pé da letra (0.16.0)** (2026-09-16; item `sih:outputschema` do monitor, a metade (B) que a decisão 37 deixou como item próprio). **O que mudou no contrato publicado:** cada ferramenta passa a anunciar `outputSchema` no `tools/list` — JSON Schema escrito à mão em `src/output-schemas.ts` e servido verbatim por `fromJsonSchema`, exatamente como o `inputSchema` (molde do bcb-br-mcp; NÃO derivado de zod). O envelope de `withProvenance` (`provenance` + `attribution`) é obrigatório em todos; o bloco de proveniência é a projeção `concise` do contrato v1.0 (seis campos, `data_vintage` e `license` anuláveis), um bloco nas sete ferramentas de fonte única e um array nas cinco multi-procedência (`get_icsap`, `get_icsap_indicators`, `rank_csap_groups`, `compare_icsap_trends`, `get_hospitalization_rates`), como `provenanceFor` já devolvia. Cliente com `tools/list` em cache rejeita campo novo até refazer o handshake — não é defeito. **De onde vieram as formas — medidas, não inventadas:** 101 chamadas contra o servidor de verdade (transporte em memória, fixture 2023/RR): os 36 casos do golden, os 27 cheio/magro da suíte e 38 variantes de `group_by`, filtro, universo e ano ausente, com um inferidor de forma que conta presença e tipos por caminho. Confirmou a medição de 14/09 e a corrigiu num ponto: a forma NÃO varia com `group_by` (uma linha com só as colunas pedidas), varia com o **caminho de erro-mole** — e ele existe em MAIS lugares do que a decisão 37 contou. Além do "ano sem dado" do funil (decisão 38, `{error, data: [], available_sih_years, note}`), são sucesso com `structuredContent` (não `isError`): o `catch` de cada handler (`{error, data|series|ranking|years: []}`), o grupo CSAP inexistente (`{error}`), a cobertura populacional das duas ferramentas de taxa (`{error, data, population_years[, available_sih_years]}`) e a resposta sem população carregada. **A forma do esquema, e por que assim:** todos os `properties` no topo, com descrição; o `anyOf` só carrega `required` — um ramo por FORMA (caminho feliz exige os campos de dados; erro-mole exige `error`; `list_csap_groups` tem três). Cliente que só renderiza `properties` vê tudo; validador aplica a exclusividade: resposta sem `summary` e sem `error` não cumpre ramo nenhum e reprova. Topo fechado (`additionalProperties: false`), linhas fechadas; mapas com chave dinâmica (ano em `get_available_years`, UF/grupo em `series` de `compare_icsap_trends`, `trends`) só tipam o valor. `filters_applied` reusa as `properties` do `inputSchema` da própria ferramenta — o eco dos argumentos é descrito uma vez. **Honestidade que a fixture não mostra e o código sim:** raça/cor anulável (nula em 1998–2007), município anulável (nulo em 1992–1997), `csap_group` anulável nas linhas (24.418 nulos no cubo de causas de 2023: as internações não sensíveis), `exclusion` anulável, medidas de `get_hospitalizations` `["number","null"]` (recorte vazio devolve uma linha nula), `first_year`/`last_year` de `data_range` opcionais (sem cubo local, `years[0]` é `undefined` e a chave some no fio), `trends` ausente com `include_trend_line: false`, `summary` de `compare_icsap_trends` com os três campos opcionais, `truncated`/`notes` de `capData` opcionais em toda ferramenta com `data`. `rate_per_100k` fica com o nome histórico e a descrição diz que a base está em `rate_per` — renomear seria outro bump. **Anunciado, não imposto em runtime:** `server.ts` registra o `outputSchema` com um validador permissivo (o `passthroughSchema` do bcb). Validar no servidor transformaria descompasso entre esquema e resposta em erro para o usuário no chat; o lugar de pegar isso é o CI. **O gate (tests/output-contract.test.ts, agora o do ilo ao pé da letra):** os 27 casos cheio/magro validam o `structuredContent` — serializado por JSON antes, como o cliente vê — contra o `outputSchema` lido do `tools/list`, com o `CfWorkerJsonSchemaValidator` do SDK (o de ajv não roda no container); 9 casos de erro-mole (2030 nas oito de dado, `g99` em `list_csap_groups`) validam o segundo ramo; a prova de que o portão fecha tem três lâminas — esquema desonesto (`csap_group` como `string` contra a resposta em que vem nulo) reprova nomeando o campo, resposta mutilada (sem `summary`) reprova, chave intrusa reprova nomeando-a; e o teste de cobertura, derivado do `tools/list`, exige de toda ferramenta `outputSchema` com `type: object`, `provenance`/`attribution` no `required` e ao menos uma forma. 84 testes; o conferidor de envelope de 14/09 continua, porque vigia o que o esquema não vê (texto == estrutura). **Gates que mudaram:** `surfaceOf` do smoke passa a capturar `outputSchema` (baseline regravado: 20,8 KB → 127 KB pretty-printed); golden só muda a versão na citação; os demais seguem iguais (a resposta não mudou em nada — só o que se promete sobre ela). **Peso:** 58,6 KB de `outputSchema` minificado nas doze (4,9 KB por ferramenta; `get_available_years` 9,9 KB, `get_hospitalization_rates` 7,9 KB), o mesmo total que o bcb com 17 (3,4 KB por ferramenta) e bem acima do ilo (0,7 KB). O `inputSchema` é 8,3 KB. O esquema de saída vai ao cliente no `tools/list` e não ao modelo (os clientes conhecidos passam ao modelo nome, descrição e `inputSchema`); se algum cliente vier a enviar o `outputSchema` ao contexto, as descrições por campo são o primeiro lugar a enxugar. **Superfície publicada muda → minor:** 0.16.0; `server.json` e golden acompanham.

40. **`classify_as_csap` deixa de responder sobre o que não é CID-10 — e passa a aceitar as duas notações do mesmo código** (2026-09-24; item `mcp:ausencia-com-200` do portfólio, frente do sih). **Os dois defeitos, medidos na produção pela rota privada:** (a) `classify_as_csap(["ZZZZ"])` devolvia `is_csap: false, csap_group: null` — lido por quem pergunta, é uma afirmação CLÍNICA ("essa condição não é sensível à atenção primária") sobre uma string que não nomeia condição nenhuma; (b) achado ao escrever a guarda do (a), e pior que ele: **`J18.1` saía `is_csap: false` e `J181` saía `is_csap: true`**, para a mesma pneumonia bacteriana do grupo g06. O ponto era removido só do lado da LISTA (`groupCid.replace(".", "")`) e nunca do lado da ENTRADA, então código dotado jamais casava com um código de 4 caracteres da Portaria. Confirmado também em `J15.3`/`J153` (g06) e `E10.2`/`E102` (g13). É falso negativo clínico e PLAUSÍVEL — não parece errado —, e a forma dotada é justamente a que a OMS escreve e a que `cid10_lookup` devolve. **Conserto (a): `is_csap: null`, nunca `false`.** `ehCodigoCid10()` decide ANTES de classificar, e o universo vem da tabela que este servidor já carrega (`src/data/cid-chapters.json`, CID-10 2019 OMS/CBCD): forma (letra + 2 dígitos, com subcategoria opcional, com ou sem ponto) MAIS pertinência a um dos 22 capítulos — assim `D49`, `V00` e `H96` são recusados, embora tenham forma perfeita. Lista escrita à mão envelheceria calada; derivar da tabela faz a guarda acompanhar a fonte. A comparação de intervalo é lexicográfica de propósito: os extremos dos capítulos são todos de 3 caracteres com dígitos zero-preenchidos (`A00-B99`, `V01-Y98`), então a ordem das strings É a ordem da classificação. **Conserto (b):** o ponto é NOTAÇÃO, não dado — os dois lados passam a ser limpos. **O resumo também mentia no atacado:** `non_csap` somava os não-classificados, dizendo que eram condições não sensíveis. Agora `non_csap` conta só o que foi classificado, `not_classified` aparece quando houver, e a resposta carrega `error` no topo — caminho de erro-mole, no padrão da decisão 38 — para o cliente que só lê o resumo não concluir que todos foram classificados. **Forma da resposta:** o servidor mantém o padrão dele (sucesso carregando `error`), não `isError` — decisão do dono na abertura da sessão: uniformizar a forma entre os quatro servidores viraria mudança de contrato em dezenas de ferramentas e não é o defeito em questão. **Gate:** `tests/cid-nao-classificavel.test.ts` (11 casos) pelo servidor de verdade em transporte de memória, com as bordas dos capítulos, as três notações, a lista MISTA (uma resposta por entrada, resumo que não mistura as três) e os pares dotado/não-dotado. **Superfície muda** (descrição + `outputSchema` com `is_csap` anulável, `error` por entrada, `not_classified` e `error` no topo): baseline do smoke regravado; o golden NÃO muda — seus 7 códigos são todos CID-10 válidos e nenhum é dotado, prova de que o conserto não mexeu no caminho feliz.
41. **A ausência de ano passa a ter UMA resposta — a guarda do bloco de cache saiu, a frase virou função e o funil ganhou `published_years`** (2026-09-25; ordem do dono ao fechar a sessão de 24/09: atacar `sih:duas-guardas-de-ausencia`; a escolha entre as três opções custeadas foi dele, na abertura: apagar a guarda antiga E unificar a frase, em vez de só apagar ou só converter). **O defeito, medido em 24/09/2026 ao conferir em produção a decisão anterior:** `get_hospitalizations({year:[1800]})` tinha DUAS respostas de formatos diferentes, e quem escolhia era o estado do disco do contêiner. Com o disco VAZIO (contêiner recém-acordado) falava uma guarda antiga dentro do bloco de cache — `isError: true`, `{error, published_years}`, sem proveniência, sem `note`, com a frase "Ano(s) X não publicado(s) no canal de cubos (...) e nenhum cubo local" —; com UM cubo qualquer em disco falava o funil da decisão 38 — sucesso honesto, `{error, data: [], available_sih_years, note}` mais proveniência. Reproduzido localmente alternando SÓ a pasta de dados: 8 das 8 ferramentas que aceitam ano divergiam. Não é ausência mentida (as duas dizem que 1800 não existe); é inconsistência de contrato — cliente que ramifica em `isError` se comporta de dois jeitos para a mesma pergunta — e, desde que a telemetria lê o envelope (24/09), a saúde do `sih` no painel oscilava com o ciclo de vida do contêiner. **Por que o caso frio era o COMUM, não o raro:** os dois resumos pré-agregados (PLAN-005/006) cobrem os 34 anos sem deixar cubo no disco, então `getAvailableYears()` fica vazio até alguém fazer uma pergunta fina; o contêiner dorme e cada despertar volta ao frio. **Por que apagar a guarda antiga não perde nada (medido, não suposto):** o gatilho dela era subconjunto estrito do do funil — `unavailable` só recebe ano fora do manifesto, que nunca está em `atendiveis`; `TOOLS_WITHOUT_CUBES` exclui as duas igualmente; e o sinal de canal caído não morava nela: `ensureYears` lança "manifesto dos cubos indisponível" ANTES de qualquer guarda, e continua lançando. Risco latente registrado e hoje inativo: se um dia um resumo cobrir ano que o manifesto não publica, o funil responde e a guarda antiga recusaria — mais um motivo para ela não existir. **O item estava errado em dois pontos, e a medição corrigiu antes de codar:** (a) não eram duas guardas, eram SEIS sítios com quatro formatos — a antiga (única `isError`), o funil, uma cópia palavra por palavra dele em `get_hospitalization_rates`, uma redação própria em `compare_icsap_trends` ("Nenhum dos anos no intervalo X-Y", sem `note`), e as duas guardas de cobertura POPULACIONAL (`population_years`, sem `note`), que respondem outra pergunta e ficaram com seu campo; (b) a resposta fria NÃO omitia a lista de anos — trazia `published_years`, a verdade do CANAL, que o funil não tinha. **Conserto:** `respostaSemAnos(anosPedidos, atendiveis, chave)` em `src/tools.ts` é a única fonte da frase "Nenhum dos anos solicitados (...) tem dados SIH disponíveis." e devolve `{error, data|series: [], available_sih_years, published_years?, note}` — o funil e as duas defesas em profundidade dos handlers a chamam; `published_years` sai de `cachedCubesManifest()` (o memo, nunca a rede) e só existe com o cache ligado, porque sem canal não há manifesto; `available_sih_years` continua sendo o que ESTA instância responde agora (pode ser `[]` num contêiner frio) e as duas listas ficam nomeadas pelo que são. As duas guardas populacionais ganharam `note` apontando `get_available_years (population_years)`. A guarda antiga foi apagada e `ensureYears` só descarta o ano fora do manifesto. **Por que sobreviveu 10 dias com um teste dedicado à classe:** `tests/ano-ausente.test.ts` afirma os invariantes certos — inclusive `available_sih_years` como array, que a guarda antiga reprovaria —, mas roda com `SIH_CUBES_CACHE=off` e a fixture de 2023 no disco: sempre no caso quente. **Gate novo, escrito ANTES do conserto e reprovando:** `tests/ano-ausente-frio.test.ts` — cache LIGADO, `SIH_DATA_DIR` e `SIH_CACHE_DIR` em pastas temporárias vazias, manifesto injetado por `setCubesManifestForTests` (o excerto versionado mais um ano), URL do canal apontada para a porta discard (a rede nunca responde), e as 8 ferramentas chamadas com 2030: `isError` falso, `error`, `available_sih_years`, `published_years` com o ano publicado, `note`, proveniência e attribution — e a prova de que nada foi baixado. As variáveis vêm no topo do arquivo e os módulos por `import()` dinâmico, porque são lidas na carga e o `vitest.config.ts` já as fixou para o caso quente. **Superfície:** `published_years` entrou no `outputSchema` das 8 ferramentas de dado (ramo de erro-mole); por isso MINOR, 0.18.0. Golden: 30 diferenças, todas a versão na citação. O worker perdeu a frase antiga da guarda de vocabulário (`worker/tests/call-shape.test.ts`); o classificador da frota não mudou. A frequência que o item pedia para medir primeiro é IMPOSSÍVEL pela telemetria atual (o funil sai `ok` com classe vazia, indistinguível de sucesso; `blob8` vazio de propósito) e não decidiria nada: ~4 chamadas/dia sem a rota do dono. O que decidiu o tamanho foi o código.
42. **1.0.0 — a superfície vira contrato; o servidor ganha ícone, site e remoto no manifesto** (2026-09-25; item `mcp:mcpindex-100` do monitor, decisão do dono na abertura da sessão). **O que foi medido:** a nota do mcpindex é a soma de cinco parcelas (`lib/quality.ts` do próprio índice), e este servidor estava em **85** perdendo 5 de `stability` (10 só para semver >= 1.0.0; 0.x vale 5) e 10 de `completeness` (5 por `websiteUrl`, 5 por `icons[0].src`), porque o `server.json` não declarava nenhum dos dois; a landing já apontava `/icon.png` e o Worker respondia **404** — ilo, uis e bcb servem o ícone com 200. **A decisão anterior era outra:** em 01/09 (item do recrawl do mcpindex, no monitor) ficou registrado NÃO buscar os pontos de `stability`, por serem compromisso de API e não ajuste de placar. O dono reverteu em 25/09 com os custos na mesa: as 12 ferramentas têm `outputSchema` e gate de contrato de saída desde a 0.16.0 (decisão 39), o golden e o smoke prendem a superfície, e a convenção "superfície muda → MINOR" segue valendo — o que o major afirma é que quebra de chamador passa a ser major. **O que mudou:** (a) `worker/src/icon.ts` é a fonte única dos bytes (PNG 512x512, leito hospitalar e cruz, figura `#30C5CC` sobre `#083B6A` — paleta dos irmãos; **sem emblema do SUS nem do DATASUS**, porque o servidor não é afiliado a nenhum dos dois, mesma linha do uis com a UNESCO), servido pela rota pública `/icon.png` do Worker, ANTES da auth — a rota fica no Worker e não no contêiner porque é o Worker quem responde ao domínio; (b) `src/server.ts` declara `websiteUrl` e `icons` no `serverInfo` do handshake (campos da revisão 2025-11-25) a partir de `SERVER_WEBSITE_URL`; (c) `server.json` ganha `websiteUrl`, `icons` e `remotes` (streamable-http em `/mcp` — não pontua, mas é o que faz o índice listar "Remote endpoint" entre as formas de instalar); (d) `worker/tests/icon.test.ts` prende os três lugares: IHDR real do PNG contra `mimeType`/`sizes` do manifesto e do `serverInfo`, mesma URL nos três, rota antes do `checkAuth`, sem segunda cópia em `assets/`, teto de 1 MB do Smithery. **Prova:** 97 testes na raiz, 145 no Worker (6 novos), typecheck dos dois, `smoke:stdio` e `smoke:http` verdes; o golden mudou só na citação (`v0.18.0` → `v1.0.0`, as mesmas 30 linhas), **zero diferenças fora da versão** antes de regravar o baseline. **O que NÃO muda:** nenhuma ferramenta, esquema ou resposta. A nota do índice mede a FICHA do registro, não o produto; a prova de que a ficha subiu é a API do mcpindex no dia seguinte ao crawl diário (`stability: 10`, `completeness: 25`), e a rodada semanal do monitor a grava em `data/mcp_quality.csv`.

## REGRAS PARA DADOS POPULACIONAIS

### Hierarquia de fontes
1. **Primeiro:** Usar dados que o IBGE disponibiliza
2. **Segundo:** Se IBGE não tiver, usar dados que o DATASUS disponibiliza
3. **Nunca:** Criar dados que nem IBGE nem DATASUS disponibilizam

### Proibições absolutas
- ❌ **PROIBIDO interpolar** dados entre anos
- ❌ **PROIBIDO incluir projeções futuras**: o último ano de população é o
  último ano de CUBO FECHADO do SIH (janela de competências completa), nunca
  além. Regra reescrita em 2026-09-06 com o sim do usuário (antes dizia
  "após 2024"): com o cubo de 2025 fechado, a população vai até 2025; 2026
  só quando o cubo de 2026 fechar (competência 2027-04 publicada).
- ❌ **PROIBIDO inventar** valores não existentes nas fontes

### Operações permitidas
- ✅ Baixar dados exatamente como estão nas fontes
- ✅ Padronizar formatos (códigos de município, nomes de variáveis)
- ✅ **Agregar municípios para obter UF** (soma simples, não é interpolação)

---

## Estrutura dos Dados Populacionais (GERADOS)

### Arquivo 1: `pop_municipios.parquet` ✅

Dados municipais consolidados de todas as fontes (IBGE via DATASUS).

| Coluna | Tipo | Descrição |
|--------|------|-----------|
| year | int | Ano (1991-2024) |
| source | string | Fonte: "censo", "contagem", "estimativa" |
| municipality_code | string | Código IBGE 6 dígitos |
| uf | string | Sigla UF |
| sex | string | "M", "F" ou "total" |
| age_group | string | Faixa etária (ex: "0-4", "5-9", ..., "80+") |
| population | int | População |

**Estatísticas:** 6.326.761 registros, 27 UFs, 1991-2024

### Arquivo 2: `pop_uf.parquet` ✅

Projeções por UF do IBGE (Projeção da População **Revisão 2024**, planilha oficial `projecoes_2024_tab1_idade_simples.xlsx` do FTP do IBGE — a SIDRA 7358 só tem a revisão 2018; desde 2026-09-07), com sexo e idade simples. Vai até o último ano de cubo FECHADO do SIH (regra acima); o servidor lê o intervalo do arquivo (`getPopulationCoverage()`), nunca o fixa. Desde a 0.12.0 os três arquivos vêm do canal `sih/cubos/` (bloco `population` do manifesto; produtor `build-population.R` no healthbr-data) e ficam no cache local; uma pasta de dados com `pop_uf.parquet` tem precedência (decisão 28).

| Coluna | Tipo | Descrição |
|--------|------|-----------|
| year | int | Ano (2000 até o último cubo fechado; 2025 desde 2026-09-06) |
| uf | string | Sigla UF |
| sex | string | "M" ou "F" |
| age | int | Idade simples (0-90, onde 90 = 90+) |
| population | int | População |

**Estatísticas:** 127.764 registros, 27 UFs, 2000-2025 (4.914 linhas por ano = 27 UFs × 2 sexos × 91 idades)

### Arquivo 3: `pop_uf_agregado.parquet` ✅

Para anos onde não há projeção UF com idade (antes de 2000), agregado dos dados municipais.

| Coluna | Tipo | Descrição |
|--------|------|-----------|
| year | int | Ano (1991-1999) |
| uf | string | Sigla UF |
| sex | string | "M", "F" ou "total" |
| age_group | string | Faixa etária |
| population | int | População (soma dos municípios) |

---

## Ferramentas MCP

| Ferramenta | Status | Descrição |
|------------|--------|-----------|
| `get_hospitalizations` | ✅ | Contagens de internações |
| `get_hospitalization_trends` | ✅ | Séries temporais |
| `get_hospitalization_rates` | ✅ | Taxas por 100 mil hab (anos 2000-2024) |
| `compare_regions` | ✅ | Comparação entre regiões |
| `get_icsap` | ✅ | Internações ICSAP |
| `get_icsap_indicators` | ✅ | Indicadores ICSAP |
| `compare_icsap_trends` | ✅ | Tendências ICSAP com taxas (anos 2000-2024) |
| `rank_csap_groups` | ✅ | Ranking grupos CSAP |
| `classify_as_csap` | ✅ | Classifica CID como CSAP |
| `list_csap_groups` | ✅ | Lista grupos CSAP |
| `list_cid_chapters` | ✅ | Lista capítulos CID |
| `get_available_years` | ✅ | Anos disponíveis |

**Total:** 12/12 implementadas ✅

---

## Estrutura do Projeto

```
sih-br-mcp/
├── data/                            (gitignored; sem sidecar versionado desde a 0.11.0; população opcional — tem precedência se houver pop_uf.parquet)
├── ~/.cache/sih-br-mcp/cubos/       (cache: cubos + sidecar por ano E pop_uf/pop_uf_agregado/pop_municipios + pop_provenance.json, baixados do canal)
├── scripts/
│   ├── freshness-check.mjs          (frescor ao vivo / selftest / excerpt; o produtor também usa)
│   ├── tables-check.mjs             (src/data/ × manifesto do canal, SHA-256)
│   ├── smoke-stdio.mjs, smoke-http.mjs, lib/smoke-checks.mjs (mesma régua para os dois transportes)
│   ├── golden-tools.mjs, cache-selftest.mjs
│   ├── warm-cache.mjs               (pré-aquece população/cubos; roda na construção da imagem)
│   └── estudo-*.{py,mjs}            (evidência das analise-002/003)
├── Dockerfile, .dockerignore        (imagem do container: dist/http.js + população — PLAN-004)
├── worker/                          (Worker de borda na Cloudflare: sih.sidneybissoli.com → container)
├── src/data/                        (CÓPIAS das tabelas do produtor + brazil-regions, cid-chapters)
├── .github/workflows/
│   ├── ci.yml                       (smoke stdio + smoke http + golden + selftests + tables:check)
│   └── publish.yml
├── src/
│   ├── tools.ts                     (as 12 ferramentas + callTool(), o despacho único dos dois transportes)
│   ├── server.ts                    (factory createServer(): McpServer v2, títulos, anotações — decisão 29)
│   ├── index.ts                     (entrada stdio, bin do npm)
│   ├── http.ts                      (entrada Streamable HTTP: /mcp e /healthz — o container)
│   ├── cache.ts, freshness.ts, provenance.ts
│   ├── db/duckdb.ts                 (queries DuckDB + população)
│   └── utils/                       (age-groups, population)
└── (produtor dos cubos E da população: healthbr-data/scripts/pipeline/sih-cubos/ — rebuild-sih-cubes.yml, build-sih-population.yml)
```

---

## Ambiente

- **Sistema:** Windows + PowerShell
- **Projeto:** `C:\dev\mcp\sih-br-mcp`
- **R:** nenhum aqui desde a 0.12.0. Cubos e população são construídos no healthbr-data (`scripts/pipeline/sih-cubos/`): `Rscript rebuild-cubes.R --years 2023 --ufs RR --out <dir>` / `gh workflow run rebuild-sih-cubes.yml -f years=2023` (decisão 27); `Rscript build-population.R --out <dir>` / `gh workflow run build-sih-population.yml` (decisão 28)
- **Node:** TypeScript (tsc), SDK v2 (`@modelcontextprotocol/server`, `/node`, `/client` — desde a 0.13.0, decisão 29), `@duckdb/node-api`

---

## Referências

- **DATASUS População:** http://tabnet.datasus.gov.br/cgi/ibge/popdescr.htm
- **SIDRA IBGE:** https://sidra.ibge.gov.br/
- **Projeções IBGE:** https://www.ibge.gov.br/estatisticas/sociais/populacao/9109-projecao-da-populacao.html
- **Pacote csapAIH:** https://github.com/fulvionedel/csapAIH
- **FTP DATASUS:** ftp://ftp.datasus.gov.br/dissemin/publicos/IBGE/
